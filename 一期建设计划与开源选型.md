# 一期建设计划与开源选型

更新日期：2026-09-16。状态：基于需求与官方文档的建议方案；尚未在目标机器人部署或测试。本文的稳定性评价指选型取向，不代表已取得真机性能证据。

最新优先级：用户要求软件架构先行。先按[软件架构设计 v0.3](软件架构设计.md)、[Galbot 对照](Galbot设计对照.md)和[动作执行契约](动作执行契约.md)明确边界、接口与时序，再开展部署和单臂实验。开源选型服务于架构，不能代替职责与契约设计。

## 1. 范围与前提

已确认需求、设备和未知项只维护于[项目上下文](项目上下文.md)。本计划覆盖语音取饮料、轮式移动、灵巧手抓取、示教/VLA、统一执行与日志；不把用户已有底层控制等同于 ROS 上层已实现。

2026-09-16 新增依据：[历史代码盘点](历史代码盘点与复用评估.md)。已有插值器、cuRobo 适配、归档 OCS2/ros2_control/模型、任务与采集等资产；补充找到连续 IK、VR、ROS 1 导航、软管 YOLO/FoundationStereo 和 ACT，已分类到[精选包](代码参考包/README.md)；后续按“复用、适配、补齐”实施，不能再将全部基础能力视为从零建设。它们尚未通过当前 Orin 和新执行契约验收；详细缺口只在盘点报告维护。

固定取物区、返回起点及返回后持物是建议场景，最终地点/交付方式待确认。实施建议以当前任务、已有接口的适配成本和可验证性为准；尚无实测证据证明某组合是最优方案。

## 2. 软件基线

优先 ROS 2 Humble，匹配 Ubuntu 22.04；截至本次核对，Humble 支持期到 2027 年 5 月。这适合作为现有 Ubuntu 22.04 的条件性基线；截至 2026-09-16 距支持结束不足一年，不应把它视作长期冻结版本。首次锁版时记录升级窗口，依据硬件/驱动验证再迁移，不直接刷机。[ROS REP 2000](https://raw.githubusercontent.com/ros-infrastructure/rep/master/rep-2000.rst)

Orin 的具体型号、内存、JetPack/L4T/CUDA/TensorRT 版本必须记录。Isaac ROS 选择与平台匹配的版本：release-3.2 官方列出 Orin、JetPack 6.1/6.2，并提供 Humble 环境，可作为候选组合；不能直接把最新主分支与现有系统混装，也不自动升级或刷写机器人。[Isaac ROS 3.2 平台说明](https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_common/index.html)

部署原则：ROS 核心组件优先发行版二进制包；视觉、语音等本地模型隔离环境，VLA 本地只部署客户端，推理环境在远端；固定包版本、源码提交、模型校验值、配置与容器镜像身份。统一提供启动、健康检查、停止、回放和最小验证入口。

## 3. 开源选型与仍需建设的部分

| 能力 | 候选与首期取舍 | 我们需要完成的工作 |
|---|---|---|
| 语音识别 | FunASR + SenseVoiceSmall + FSMN-VAD | 麦克风接入、语音片段管理、饮料名称测试、ROS 2 包装 |
| 指令解析 | 首期自建受约束的饮料词表和意图解析器 | 文本映射为技能目标，处理别名、否定和歧义；自由语言需求超出规则覆盖后再评估模型 |
| 已有地图导航 | Nav2 + AMCL | 地图接入、里程计/TF、底盘速度适配、足迹、避障、到站检查、取消和恢复 |
| 三维雷达接入 | pointcloud_to_laserscan + Nav2 Voxel Layer | 定位用高度切片；原始三维信息用于适当高度的障碍检测；过滤地面与机器人自身点云 |
| 饮料检测/分割 | Ultralytics YOLO 分割模型 | 以 YOLO11n-seg 为首个轻量候选，固定版本；采集目标饮料数据并微调，评估后导出部署引擎 |
| 物体位姿 | Isaac ROS FoundationPose | RGB-D、内参、目标 mask、对象 mesh/纹理、单位与坐标；延迟、失效和对称性处理 |
| FK/IK/规划 | 已有 cuRobo/OCS2 候选与 MoveIt 2 + KDL + OMPL 比较 | 优先核对归档模型和现有适配；验证构建、运动学/碰撞、连续性、时间和资源后选首个后端 |
| 抓取技能 | 复用历史位姿序列/手型模板，适配统一规划或 VLA 策略 | 掌心抓取位姿、手指预形、接近、受约束闭合、抬升、持物确认、运输姿态和恢复 |
| 灵巧手与遥操作 | 厂商 SDK；需要人体手姿映射时评估 dex-retargeting | 手部模型、驱动耦合、具名关节映射、臂手协调、真实反馈；重定向不能代替接触与持物验收 |
| 执行后端 | 优先复用现有 SDK；ros2_control 依能力选用 | 先验证真实反馈、排队/插值、停止与周期，按架构 7.2 选择一个后端 |
| 轨迹处理 | 优先评估已通过有限离线场景的 C++ 插值内核；JTC/Ruckig 按缺口引入 | 仍需多轴时序、断流/停止、边界与反馈验收；不默认叠加多套滤波，见 3.5 及盘点报告 |
| 技能编排 | 首个固定任务采用显式状态机 + ROS Action；行为树为后续候选 | 保留取消/验收/恢复语义；组合复杂后再评估 BehaviorTree.CPP / ROS2 集成 |
| VLA 接入 | Pi 历史 ZMQ 两模式接入；ACT 暂缓，OpenPI 仅作协议参考 | 机器人观测与动作布局、动作块拼接、过期裁剪、100 Hz 转换、未控关节语义、执行反馈 |
| 原始采集 | rosbag2 + MCAP | episode 管理、时间戳、采集选择、丢帧统计、记录配置和遥操作事件 |
| VLA 数据处理 | LeRobot | 原始数据到模型 schema 的转换、动作标签定义、数据质检、版本与来源追踪 |
| 日志与诊断 | ROS diagnostics + rosbag2 + PlotJuggler + 自建结构化事件 | 统一运行身份、观测/请求/命令关联、资源和控制指标、轮转归档与故障时间线 |

上述模型和库不是都自带完整 ROS 2 技能接口。包装层属于计划交付物，避免业务层直接耦合其内部 Python/C++ API。

### 3.1 语音：先完成“说出饮料名称就能发起任务”

FunASR 提供 SenseVoiceSmall、VAD 及 CPU/GPU 用法，也有单独的 Paraformer 流式路径。首期建议一句话结束后识别，优先在 CPU 评估以减少 GPU 资源竞争；只有交互延迟需要时再启用流式配置。ASR 负责文字识别，任务解析和歧义处理仍需自建。[FunASR](https://github.com/modelscope/FunASR)

### 3.2 导航：先核实地图和里程计

首选是 Nav2 的已有二维地图导航。三维雷达可通过高度筛选转换为 LaserScan；三维障碍信息保留给 Voxel Layer。这个选择针对平地室内场景，并不意味着已有三维点云地图能直接作为 AMCL 地图。[Nav2](https://docs.nav2.org/)、[点云转扫描](https://github.com/ros-perception/pointcloud_to_laserscan)、[Voxel Layer](https://docs.nav2.org/rolling/configuration_and_development/configuration_guide/core_servers/costmap_2d/costmap_plugins/voxel/)

必须确认底盘是否提供可靠里程计、雷达是否刚性安装、已有地图格式。若缺少 odom，先增加里程计模块建设，再根据雷达型号和同步条件选型，不默认相机内置 IMU 可以替代底盘里程计。不把未知 ROS 2 移植版的 LIO 直接作为稳定部署基线。

Voxel Layer 内部使用三维观测并投影到二维代价图，不等于全身三维运动碰撞检查；运输包络与操作场景仍需验证。上面的 Voxel 页面为当前 Rolling 概念参考，参数必须对照实际 Humble 版本。旧仓库链接本轮返回 404，已更换；[Nav2 里程计说明](https://docs.nav2.org/jazzy/configuration_and_development/first_time_robot_setup_guide/odom/setup_odom_gz/)仅用于 odom/TF 语义，不复制其 Jazzy/Gazebo 启动配置。

导航技能完成条件包括：实际位姿到站复核、速度收敛、手臂运输姿态有效；地图路径到达与具备抓取站位分别判断。持物状态需影响导航速度和整机包络配置。

### 3.3 感知：饮料身份、物体位姿和抓取点分别建设

YOLO 可检测/分割并支持训练与导出。通用类别不能作为具体品牌或 SKU 的识别验收依据；一期建立有限饮料目录、标签与测试集。[Ultralytics](https://github.com/ultralytics/ultralytics)

Isaac ROS FoundationPose 提供 ROS 输入和 TensorRT 路径；模型模式需准备对应 mesh、纹理、彩色/深度图、内参与 mask。估计与跟踪分别验收；复杂对象初始化可能较慢，建议到站后初始化并按需跟踪，用实际 Orin 测量延迟。[FoundationPose ROS 3.2](https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_pose_estimation/isaac_ros_foundationpose/index.html)

模型资产、相机标定、手眼标定、深度对齐、对称物体姿态处理、透明/反光物体上的实测都属于交付。固定直立瓶罐可提供深度几何与预设抓取模板作为替代策略，但保留 FoundationPose 的建设与验证范围。

### 3.4 规划：不要求从零实现 IK 和避障

规划后端负责运动学/碰撞/路径能力，自建技能负责饮料的抓取语义和结果。已有 cuRobo/OCS2 代码应先核对依赖与模型，再与 MoveIt 候选在统一目标和执行合同下评估；不依据目录名确定最终路线。若选择 MoveIt 2，对受支持的串联臂运动组先用 KDL 与 OMPL 建立基线；灵巧手耦合/闭链按模型单独处理。按目标样本测量可达率、解连续性与规划时间。路径时间参数化与平滑可以改变几何，应在处理后验证碰撞与路径偏差。[Humble 运动学配置](https://moveit.picknik.ai/humble/doc/examples/kinematics_configuration/kinematics_configuration_tutorial.html)、[Humble 轨迹处理](https://moveit.picknik.ai/humble/doc/examples/time_parameterization/time_parameterization_tutorial.html)

### 3.5 执行组件的选取依据

实现分支见[架构 7.2](软件架构设计.md)，具体执行语义只维护在[执行契约](动作执行契约.md)。2026-09-16 复核结论：

- **MoveIt 与控制器解耦**：可以接 FollowJointTrajectory 适配，不强制采用 ros2_control。先复用现有控制器；文档中的部分旧 launch/debug 示例带 TODO，不能当作已验证 Humble 部署命令复制。[Humble 控制接口](https://moveit.picknik.ai/humble/doc/examples/controller_configuration/controller_configuration_tutorial.html)
- **JTC 有限轨迹基线**：默认 spline 下，位置-only 为线性插值；加入一致的速度/加速度可获得不同阶连续性。Humble 页面说明轨迹替换会丢弃旧轨迹，因此不能直接认定它保留我们定义的锁定前缀/未来段拼接。锁定具体包版本后验证其行为，不能混用 ROS 1 或最新分支文档。[Humble JTC](https://control.ros.org/humble/doc/ros2_controllers/joint_trajectory_controller/doc/trajectory.html)
- **Ruckig 的边界**：Community 可做本地状态到状态的运动生成；Tracking Interface 为 Pro，中间点路径涉及云端。当前 README 的位置上下界字段也标为 Pro，所以我们仍须独立验证整条曲线的关节范围、路径偏差与碰撞；限 jerk 不自动包含这些保证。[Ruckig 官方](https://github.com/pantor/ruckig)
- **连续末端运动**：已知路径用 CartesianPath/连续种子 IK 后验收完成比例、跳变与碰撞；在线局部更新评估版本匹配的 Servo。其真实反馈、时间参数化和停止效果必须实测；不以 100 Hz 重复全局规划。[Humble Servo](https://moveit.picknik.ai/humble/doc/examples/realtime_servo/realtime_servo_tutorial.html)、[Cartesian 插值源码](https://raw.githubusercontent.com/moveit/moveit2/humble/moveit_core/robot_state/src/cartesian_interpolator.cpp)

完整旧 MoveIt 工程仍未找到，但其他历史源码已经可以开展离线复用验证，不能再写成“本机无源码”。按[盘点报告](历史代码盘点与复用评估.md)先复用独立插值、协议与模型，修复执行/采集缺口；MoveIt 取得后再核对版本/API。

### 3.6 VLA、数据与日志

用户已确认本地只运行 VLA 客户端，模型在远端推理。本批实现 Pi 的历史 ZMQ 两种客户端模式，ACT 按用户要求暂不接入；OpenPI 仅作协议参考，不据此更换已有服务协议。待核对远端服务版本、观测/语言字段、单步或 chunk 动作与手部布局。本地工作包括 ROS 2 迁移、观测构造、通信/超时、动作解码和统一执行接入，无需部署 VLA 权重或推理运行时。机器人控制权、平滑、停止和验收仍由本系统负责。[OpenPI 协议参考](https://github.com/Physical-Intelligence/openpi/blob/main/docs/remote_inference.md)

[rosbag2](https://github.com/ros2/rosbag2) / MCAP 与 [LeRobot](https://github.com/huggingface/lerobot) 为原始记录、训练格式转换候选；按目标训练端锁定 schema，不默认最新版兼容。数据标签、采集 profile 与关联规则集中在[架构第九节](软件架构设计.md)。诊断工具候选：[ROS diagnostics](https://github.com/ros/diagnostics)、[PlotJuggler](https://github.com/PlotJuggler/PlotJuggler)。

## 4. 分阶段建设与验收（灵巧手条件下修订）

当前统一使用 P0–P6 建设顺序。旧 A0–A4 合并为 P0 内设计/模拟活动；设备盘点与最小接口设计可并行。这是建议顺序，没有工期或真机性能承诺。

### 4.1 主线：先把单臂单手抓取做成可复现的能力

| 阶段 | 建设工作 | 阶段出口/完成证据 |
|---|---|---|
| P0 最小架构与软件闭环 | 定义 RobotProfile/HandProfile、真实状态、臂手目标、控制权、停止/保持及日志契约；模拟后端接固定/回放策略；完成首条纵向链所需契约和模拟验证 | 用一组具体臂手样例验证输入到反馈关联；旧代次、超时、取消、手后端拒绝都有明确处理；尚不代表真机可用 |
| P1 单臂单手执行基础 | 复用底层控制；核实手部 SDK、实际反馈、独立驱动/耦合和各自周期；URDF/TF/掌心/指尖/碰撞；先单独受限动作，再臂手协同；启用最小录制 | 臂、手分别能受控运动/停止，命令与实测可区分；映射/限位正确，反馈失效可识别；验证臂手衔接及断流行为 |
| P2 遥操作抓取与早期数据 | 接已有示教设备；需要时增加手指重定向；先有限饮料、单臂单手、已知摆放，调试手型、接近、闭合、抬升、保持、放下 | 人操作能够重复抓取/保持/释放；episode 包含同步图像、臂手动作、实测、配置及结果；形成第一份可加载的小样本数据 |
| P3 已知位置自动抓取 | 用已知物体位姿排除视觉误差；选定规划策略做手臂/掌心路径，手部模板或经过验证的轨迹做预形与闭合；技能状态机管理接触和结果 | 相同条件下自动完成接近→抓住→抬升→运输姿态→受控放下；失败能区分路径、手型、接触和持物问题 |
| P4 视觉自动抓取 | YOLO 饮料身份与 mask、FoundationPose、对象资产与手眼标定；把物体位姿变为掌心抓取候选和手指预形；有限重观察/换候选 | 在约定摆放变化下重复抓取；记录身份/位姿误差、抓取成功率及失败原因；过期或证据不足的观测不能直接触发抓取 |
| P5 移动取饮料任务 | 接导航分线成果、持物会话、运输约束与语音；先文本结构化指令联调，再接 ASR；验证返回后的持物结果 | 语音指定饮料→到站停稳→视觉抓取→持物返回；每次有明确物理结果和证据，重试/调整站位预算可追溯 |
| P6 VLA 策略与稳定化 | 在前期客户端/数据基础上联调远端服务、迭代数据并对接远端训练/适配结果，验收动作块→100 Hz→后端执行；同技能传统/VLA 替换，完善恢复和压力验证 | 训练端可加载版本化臂手数据；模型不绕过执行入口；晚到/断流/切换行为有证据；重复任务统计效果和资源竞争 |

P0 先明确首条臂手执行链的必要契约与例子，满足后即可进入 P1；其余技能接口随对应阶段细化，不要求所有未来双足/VLA 算法全部定稿。P1 中先关节受限运动，再末端短路径，再臂手协调；实际状态不可读取时，不能把发送成功当作闭环完成。旧 MoveIt 本机无源码，待取得后审查复用；不把等待旧代码作为模型/接口盘点的阻塞条件。

P2 的意义是先证明当前手型、反馈和接近方式能完成物理抓取，同时暴露自动策略将面对的困难。它不代表复制一条遥操作轨迹即可泛化；录制、离线回放和真机执行是不同入口。若设备暂不支持手指遥操作，可先使用明确的手型选择/闭合程度作为调试输入，并记录该来源；完整手指示教仍是后续数据能力缺口。

P3 建议先选少量瓶罐和一种稳定包覆抓型。手指预形表示接触前的关节姿态；抓取模板包含物体到掌心变换、接近方向、手指预形、闭合约束及适用物体范围。首期先服务于拿取与运输，复杂手内翻转、拧瓶盖和双手协作分别评估后再扩展，不自动纳入本次阶段出口。

### 4.1.1 当前实现批次：以 v3.4 遥操作验证首条执行链

2026-09-16 基于历史代码复核更新；用户已授权在指定 Ubuntu 开发机实现并验证模拟骨架，实际完成情况见[当前进度](当前进度.md)。后续真实能力接入仍按下表验收。保持 P0–P6 验收顺序，将 v3.4 明确作为近期遥操作/连续 IK 的优先复用来源。

| 顺序 | 本批工作与复用来源 | 完成条件 |
|---|---|---|
| 0 Ubuntu 可扩展骨架 | 按[架构8.5](软件架构设计.md#85-ubuntu-上的可扩展骨架首期落地边界)建立公共接口、任务/技能、观测/场景、能力适配、执行、本体后端、记录与运行装配边界；真实算法先由模拟实现替代 | 模拟取饮料流程与执行异常可运行；同技能换策略、换本体配置、能力独立进程运行、记录/观测故障有可复现实验；模拟来源明确标识 |
| 1 锁定单臂单手与环境 | 读取当前控制接口、具名反馈、停止/保持与周期；核对 Orin/ROS/JetPack、当前 URDF、手部驱动映射。v3.4 按原始工程关系恢复所需源码，先隔离模块导入时的进程/端口/硬件副作用 | 有可复现的环境与配置；关节顺序、单位、限位、掌心坐标及手部维度有来源；接口缺口明确，模拟工作不等待缺口全部消失 |
| 2 最小统一执行与观测 | 复用已有底层；先固定目标/回放与模拟后端，再受限真机动作。已有 C++ 插值内核仅用于补齐确认缺失的能力；记录输入参考、下发和实测 | 正常完成、目标替换、取消、断流、反馈过期可区分；运动控制源唯一；有最小关联日志；不得用末点发布代替实际到达 |
| 3 接入 v3.4 遥操作 | 保留 VR 相对目标、idle/init/follow、FK/IK 和必要通信；将运动输出接入同一执行入口。先核对 FK/模型、离线连续目标和跳变/限位/碰撞，再接单臂，随后接灵巧手 | 从当前姿态接入和退出遥操作，目标连续性及实际跟踪有证据；IK 耗时/失败可见，不宣称优化器一定100 Hz；手部不能被1维历史标签替代 |
| 4 抓取与数据小闭环 | 选定一种饮料、已知摆放，进行遥操作接近、闭合、试抬、保持和放下；修复历史采集保存与动作语义，同步图像/臂手参考/实测/时间/结果 | 可重复完成一次完整操作流程并统计尝试与失败；采样数据能落盘、重新加载和离线检查，记录原始输入、受约束命令与实测的区别 |

**第一个软件里程碑为 Ubuntu 单机骨架可运行；第一个真机里程碑为单臂单手遥操作抓取及可用 episode。** 软件骨架验证在真实能力接入之前完成；先单臂验证不会删除 v3.4 的双臂能力。训练动作明确选择参考或下发命令等语义，不默认用下一帧实测替代。真机回放另经执行入口验证，数据可读取不等于可以直接回放驱动机器人。

**Ubuntu 骨架的完成范围**：以[架构8.5](软件架构设计.md#85-ubuntu-上的可扩展骨架首期落地边界)为唯一职责定义，先用模拟能力串起完整任务和观测/执行/记录闭环；不把单条关节执行演示作为整套骨架完成标准。底层算法可模拟，接口、状态归属、依赖选择、取消传播和运行装配必须真实可用。逻辑模块不必各自成为进程，按依赖和时序要求组合部署。

接入顺序：公共合同与两个模拟本体配置 → 任务/技能及模拟能力组合 → 控制/观测/记录与故障验证 → 同技能换策略和独立进程替换实验 → v3.4/真实本体/灵巧手 → 规划、感知、导航、真实 VLA。已有平台边界对应模拟实现，后续逐项替换；导航分线可以独立提前推进。100 Hz 参考和自适应插值按执行契约验收，开发机结果不代表 Orin 或硬实时性能。

**近期 GUI 范围**：复用已有界面的关节反馈、目标输入、控制模式、执行状态与停止/保持交互，仅做调试面板；状态来自后端，界面不承担插值循环。地图/视觉/任务大屏后续按需要扩展。

**并行而不阻塞主线**：导航学生按4.3推进；Pi 客户端已进入 ZMQ 模拟服务/统一执行联调，后续核对真实相机和服务协议；100 Hz 参考接续及生产平滑仍按执行契约推进，真实模型策略按 P6 验收；相机与标定从第3/4步准备。全局规划器在 P3 按当前模型和平台验证后择一，不同时恢复 cuRobo、OCS2、MoveIt 三套完整执行链。

本批通过后，按既定顺序推进已知位置自动抓取（P3）、视觉自动抓取（P4）、导航与语音汇合（P5）、真实 VLA 策略与稳定性验收（P6）。物体范围、重复次数和误差/延迟阈值在首次硬件能力检查后定稿，不预填未经验证的工期或成功率。

### 4.2 灵巧手专属交付

HandProfile、掌心/指尖坐标、驱动耦合、真实反馈和臂手资源移交按[架构第四节](软件架构设计.md)与执行契约验收。首期交付一个可复现的抓取模板与阶段判据：预形→接近→受约束闭合→满足初步抓取证据→受限试抬→确认持物→运输/放下。避免把“必须抬起才能证明持物”与“必须已持物才允许抬起”写成循环条件。

若遥操作输出人体手部关键点，可评估 [dex-retargeting](https://github.com/dexsuite/dex-retargeting)，按具名关节映射；已有可用映射则直接复用。它不负责证明接触稳定，也不保证当前设备支持完整手指示教。

### 4.3 并行线与汇合条件

**导航学生建议分工：**

1. N1：Nav2 仿真完成到点、取消与避障，交付配置和复现说明。
2. N2：接真机雷达/地图/odom/TF，核实底盘速度与停止接口。缺接口时继续仿真和传感器工作，明确缺口。
3. N3：空载真机 A→B→A，交付实际到站位姿、停稳判断与失败结果。
4. N4：P3 抓取与运输姿态通过后，开展持物移动；验证不同转弯/加减速条件、手臂手部包络与持物证据，再汇入 P5。

导航与操作从 P0 就约定坐标系、时间戳、控制权与停稳/运输状态，但不要求学生等待自动抓取完成才学习和验证 Nav2。持物速度/加速度从实测确定，不把空载导航结果直接当作持物验证。此为分工建议，未对外下发任务。

**其他并行工作：**

- 日志从 P0/P1 开始，示教从 P2 开始，数据转换/加载同步进行；早期调试示教标注质量与用途，不自动混入最终训练集。
- VLA 客户端骨架从 P1/P2 用固定/记录动作块验证观测、超时、动作布局和日志；模型真机接管在动作链与数据验证后，P6 不意味着此前不开发 VLA。若专门适配模型已取得更好实测结果，可作为技能策略提前试用，仍须通过相同验收。
- 语音/词表可以独立验证；系统集成先用结构化文本指令，P5 必须接回语音完成用户要求。
- 标定、饮料资产和视觉部署可在 P2/P3 并行准备，真实闭环按阶段出口接入。

### 4.4 验收与数据记录原则

每阶段记录样本范围、尝试次数、成功定义、失败原因和证据路径；少量一次成功不表述为稳定可用。阈值和重复次数依据实际硬件/场景定稿，不预填未经验证的成功率。

- 执行：输入/下发周期、延迟与抖动，真实跟踪误差；手部耦合、限位、反馈缺失、停止/保持及跨设备偏差。
- 抓取：手型形成、接触、抬升后保持、运输后保持和受控释放分别记录；不能仅凭闭合命令判断成功。
- 视觉/导航/语音：错误饮料、位姿失效、到站但不可抓、定位异常、无效语音应有明确结果。
- 数据：保留遥操作原始手姿/输入、重定向目标、受约束参考、下发与实测；记录手部配置/映射版本、阶段和人工接管。训练 action 来源明确定义，图像与臂手状态有时间偏差和缺失统计。
- 系统：推理、感知、录制并行时的周期与资源；取消、重启、迟到动作、记录失败和持物交接可复现。恢复与错误处理随每阶段建设，P6 做系统性稳定化。

## 5. 部署前输入与性能证据

硬件/场景待输入清单集中在[项目上下文](项目上下文.md)，不在多个文档重复维护。

需要先填的能力表：臂/手命令模式与实际频率、SDK 阻塞上界、队列/不可撤销长度、真实反馈字段/年龄、停止/保持行为；底盘速度、odom 与地图格式。提供命令/反馈日志样例后再定缓冲与阈值。

Orin 验证按负载分步：执行单独运行 → 加相机与记录 → 加本地感知推理 → 加 VLA 远端客户端通信与动作处理。每步记录周期/延迟分布、CPU/GPU/内存、温度与数据丢失；不在没有目标平台证据时填成功率/性能承诺。Isaac ROS 3.2 FoundationPose quickstart 标注引擎转换需要至少 7.5 GB 空闲 GPU 内存，并说明 TensorRT 10.3+ 下的精度设置；这是构建阶段要求，不等于运行时内存。Orin 型号/内存未知时不能承诺全本地快速部署。[官方 quickstart](https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_pose_estimation/isaac_ros_foundationpose/index.html)

## 6. 项目边界

优先复用成熟算法库，自建机器人适配、公共数据契约、技能结果验收、在线执行管理和全链路追踪。首期不开发双足步态/平衡控制，不预设模型权重能跨本体直接运行。

本文延续 [control架构](../参考资料/Galbot/control架构.md) 的时间尺度分层与统一契约，参考 [YDH 架构](../参考资料/Galbot/ydh-catering-architecture.md) 的感知、运动学、技能和恢复边界，以及 [日志与系统服务](../参考资料/Galbot/日志与系统服务.md) 的生命周期和诊断组织。
