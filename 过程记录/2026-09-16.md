# 2026-09-16 过程记录

## 01 — 绘制整个系统架构图

- 目标：根据现有架构与用户确认的灵巧手条件，直观呈现系统模块、动作流、观测反馈、日志和数据链。
- 变更：新增[系统架构图](../系统架构图.md)，包含 Mermaid 系统总览、执行链放大图和部署边界；同步 README、上下文、架构入口和当前进度。
- 关键表达：任务/技能调用策略，所有运动提议进入执行管理；底盘速度独立于 100 Hz 臂手参考；灵巧手协议与周期待核实；持续持物责任与场景事实分开；数据记录旁路、训练离线，双足作为后续扩展。
- 验证：检查图中节点引用、本地文档链接和围栏配对；按现有执行契约走查候选/提交/下发/反馈与日志链。未运行 Mermaid 渲染器、ROS 节点或机器人测试。
- 结果：形成可在文档与对话中查看的架构图，未变更既有已确认范围，也未把候选软件标为已部署。
- 下一步：基于图中单臂单手执行链细化输入/反馈消息与 HandProfile 示例，补齐硬件能力后推进模拟闭环。

## 02 — 增加七层内部架构图

- 目标：回应用户“再加一版各层架构”，保留总览并逐层展开。
- 变更：新增[各层架构图](../系统架构图.md#4-各层职责展开)，包括任务技能、观测环境、导航操作策略、统一执行、硬件适配、公共运行支撑、日志数据七张图；每层附输入输出、状态负责人和边界，最后给出接口与代码归属速查。
- 一致性：沿用原七层编号；明确观测和公共支撑不构成严格串行流水线，策略不直接写硬件；速度门控与关节提交共享权限管理；第 4 层参考推进和第 5 层设备适配属于后端不同职责，不强制拆进程。
- 同步：README、总体架构、系统总览、上下文、当前进度；未新增范围决策，已有 D010/D011 仍适用。
- 验证：Python 检查本地 Markdown 链接、围栏配对、七个 flowchart 的节点定义与边引用；人工对照执行契约和各层所有权。未运行 Mermaid 渲染器或机器人测试。
- 结果：完成可继续细化接口的七层展开视图，候选软件、接口名和硬件能力仍保留原状态。
- 下一步：优先将第 4/5 层单臂单手链路转为字段级契约与能力配置示例，再接模拟后端。

## 03 — 增加硬件到应用的总体分层图

- 目标：用户希望看到“硬件层什么之类”的总体架构，将细节收敛为清晰层级。
- 变更：在[系统架构图](../系统架构图.md)第 0 节新增六层主架构：硬件、硬件抽象与驱动、控制执行、算法能力、任务技能、应用交互；ROS 2、配置运行、日志数据作为跨层平台。同步入口、上下文与进度。
- 边界：属于已有架构的高层视图，不替代七类职责展开；说明观测可直接到消费者、ROS 2 不替代硬件总线、双足是后续目标。
- 验证：检查 Mermaid 节点/边引用、围栏及本地链接，走查与既有范围的一致性；未运行渲染器、代码或机器人测试。
- 结果：形成可用于项目介绍和总体讨论的一张分层图；原详细图保留。
- 下一步：以总图确定讨论层次，需要接口细节时再转到各层展开与执行契约。

## 04 — 生成科研汇报用总体架构图片

- 目标：用户要求直接生成美观、符合科研表达的图片，无需可编辑版本。
- 变更：使用内置 image_gen 生成六层主架构与跨层公共平台 PNG，保存到[图片](../图片/总体架构图-科研版-v1.png)，保留[完整提示词和工具记录](../图片/总体架构图-科研版-v1-提示词.md)；同步总览入口、README 与当前进度。
- 验证：目视检查六个主层、三个公共平台、中文关键术语与方向箭头；核实 PNG 文件头、1536 × 1024 尺寸及保存路径。未修改生成图片像素，未进行机器人测试。
- 结果：完成适合科研汇报的静态架构图片；原文字架构保持，图片不作为算法性能或系统部署证据。
- 下一步：继续按现有计划细化单臂单手契约与硬件能力。

## 05 — 架构正确性、实现取舍与文档去重审查

- 目标：审查架构/实现路径和当前文档正确性、冗余；结果见[审查报告](#审查详情)。
- 变更：架构 v0.3、执行契约 v0.2；补齐有限轨迹正常终点、重定时权限、持物资源移交、制动包络和试抬碰撞模型；执行组件按硬件能力选择，固定任务状态机优先，复杂机制后置。记录 D012，保留用户一期范围。
- 文档治理：事实/进度/职责/执行/计划分工，P0–P6 为唯一阶段表；去除图示重复接口正文，保留图片与历史。8 份当前工作文档由 55,258 减至 44,934 字符，减少 18.7%；不将此数字称为重复率。
- 验证：官方 MoveIt/JTC/Ruckig/ROS 平台/Isaac ROS/Nav2 资料核对；修复已发现的旧外链。Python 检查本地链接、围栏、flowchart 节点引用和文本规模；确认旧日过程记录与图片提示词未改。详细来源及未覆盖范围见报告。
- 结果：完成文档审查与修订；没有源码或硬件运行测试，不能宣称性能最优或故障用例已通过。
- 下一步：取得单臂单手 SDK 能力/真实反馈样例，选定一个后端并实现最小模拟验证；导航按原分线继续。

### 审查详情

以下为当日第05项审查的原始报告正文；保留当时状态与统计口径，后续实现进展见第11项及[当前进度](../当前进度.md)。

#### 结论与范围

总体分层、硬件抽象、任务/策略/执行分离和灵巧手方向合理；现有资料不足以证明任何实现“最优”。主要问题是：完整平台设计与首期必要实现混排、若干执行边界未闭合、同一规则多处维护。本轮已修正文档，不代表代码缺陷已通过测试修复。

检查范围：`bindu/` 现有 13 份 Markdown、总体图片的既有生成记录，抽查根目录 Galbot/YDH/control 来源；核对关键控制器和平台官方资料。没有用户 MoveIt 源码、SDK 或真机访问证据，因此未做代码、硬件或全包兼容测试；没有重新生成图片。不是对全部外链可用性的全面扫描。

#### 1. 主要发现与处理

“高”表示照原文字实现可能影响动作/状态正确性；“中”主要影响实现选择与维护成本。表中原文定位指本轮修订前版本，链接指修正后的主文档。

| 编号 / 优先级 | 发现与依据 | 影响及本次处理 |
|---|---|---|
| R01 高 | 执行契约第 6 节对缓冲余量统一使用制动条件，没有区分正常最后一段 | 有限轨迹接近终点也可能被提前截断。增加显式结束标记、终端状态/容差和按模式区分的结束/断流规则 |
| R02 高 | 架构 7.3 允许按跟踪状态“调整推进速度”，同时要求唯一已提交时间轴；下层权限不明确 | 暗中变速会使 VLA 的旧前缀/切换时间失效。改为执行端提交新 revision 和生效边界，采样器只采已批准曲线；预授权停止单独处理 |
| R03 高 | 持物会话接管“灵巧手及必要保持资源”，未具体说明臂后续如何运动 | 可能永久锁臂或产生第二写入者。拆开持物责任与臂写权限，明确受控运动/确认移交；手部保持继续 |
| R04 高 | 停止设计重视速度/加速度/jerk，却未明确停止路径包络 | 平滑制动仍可能碰撞或破坏接触。补充制动距离/SDK 前缀、几何包络与阶段约束；不承诺所有故障可平滑停止 |
| R05 中 | 计划表将 ros2_control、JTC、Ruckig 并列为执行基线，正文又允许 SDK 适配 | 实现者可能串联多个插值器。改为硬件能力分支选择一个实际后端，MoveIt 使用标准轨迹入口；未来段拼接另验收 |
| R06 中 | 计划 4.2 写“持物未确认不抬升/运输”，同时又要求抬升验证持物 | 容易形成循环判据。明确初步抓取证据→受限试抬→确认保持→运输；阈值待设备/场景确定 |
| R07 中 | 完整对象/会话、十三包布局、行为树、A0–A4/P0–P6 同时出现 | 首条实现链的前置条件容易膨胀。改为三类自研职责、固定资源表和显式状态机；A 活动并入 P0；RTC/复杂并发按需增加 |
| R08 中 | 上下文收录图示变更；进度重复每轮设计历史；架构、计划、图示反复定义执行语义 | 多处同步易遗漏。明确各类信息主文档，压缩上下文/进度，图示保留但改为辅助；历史过程只追加 |
| R09 中 | 持物/场景版本规则没有说明只校验相关依赖 | 无关对象每帧更新可能令候选不断失效。增加依赖范围；相关环境失效仍需停止/重规划，不能为减少拒绝而忽略障碍 |
| R10 中 | Humble 基线正确但长期迁移窗口不突出；Orin 只给系列名；Voxel 外链已失效 | 增加支持期、构建/运行资源区别及锁版要求；修复已发现 404，当前 Nav2 页面只作概念引用，不跨版本复制参数 |
| R11 中 | “丰富日志”未形成具体采集负载边界 | 完整策略证据与所有硬件周期永久录制容易混淆。加入常规、控制调试、示教 profile，记录丢失/降采样与容量预算 |
| R12 高 | 原端到端流程先抬升确认，再更新物体附着碰撞模型 | 试抬路径可能遗漏随手物体的几何。改为试抬前更新待验证的附着/接触假设并确认投影，试抬后才提交真实持物事实；失败修正占用 |

执行修正主文档：[契约 v0.2](../动作执行契约.md)。职责及实现取舍：[架构 v0.3](../软件架构设计.md)。阶段和组件依据：[一期计划](../一期建设计划与开源选型.md)。

#### 2. 官方资料核对结果

- MoveIt 支持对接自有轨迹 Action，ros2_control 非强制依赖；页面部分调试示例保留 ROS 2 更新 TODO，不能直接当部署脚本。[Humble 控制接口](https://moveit.picknik.ai/humble/doc/examples/controller_configuration/controller_configuration_tutorial.html)
- Humble JTC 按位置/速度/加速度字段采用不同插值；页面指出替换会丢弃旧轨迹。不能据此声称满足我们要求的锁定前缀与动作块衔接，仍需锁定安装版本测试。[JTC 文档](https://control.ros.org/humble/doc/ros2_controllers/joint_trajectory_controller/doc/trajectory.html)
- Ruckig Tracking 为 Pro，Community 中间点走云端；位置上下界参数亦标为 Pro。社区版状态到状态生成可评估，但路径/位置范围检查不能省略。[官方 README](https://github.com/pantor/ruckig)
- Humble Servo 的局部命令路径与现有建议相符；KDL 插件限串联链，不能把整只耦合灵巧手自动视为同一个串联 IK 组。[Servo](https://moveit.picknik.ai/humble/doc/examples/realtime_servo/realtime_servo_tutorial.html)、[运动学配置](https://moveit.picknik.ai/humble/doc/examples/kinematics_configuration/kinematics_configuration_tutorial.html)
- Humble 对 Ubuntu 22.04/arm64 的对应关系成立，支持到 2027 年 5 月；截至本轮审查不足一年，应在锁版时安排升级评估。[REP 2000 官方源码](https://raw.githubusercontent.com/ros-infrastructure/rep/master/rep-2000.rst)
- Isaac ROS 3.2 对应 JetPack 6.1/6.2；FoundationPose 构建模型引擎需至少 7.5 GB 空闲 GPU 内存。后者不是推理运行内存，不能拿来证明任意 Orin 足够或不足。[平台](https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_common/index.html)、[FoundationPose](https://nvidia-isaac-ros.github.io/v/release-3.2/repositories_and_packages/isaac_ros_pose_estimation/isaac_ros_foundationpose/index.html)
- Nav2 旧路径访问失败，现行 Voxel 说明明确三维观测投影为二维代价图；这不等于全身三维避碰。现行页面分支不同，仅核对概念。[Voxel](https://docs.nav2.org/rolling/configuration_and_development/configuration_guide/core_servers/costmap_2d/costmap_plugins/voxel/)、[odom/TF](https://docs.nav2.org/jazzy/configuration_and_development/first_time_robot_setup_guide/odom/setup_odom_gz/)

其余 ASR/YOLO/数据工具保留此前候选身份，本轮未重做全版本、许可证或性能审查。文档有引用不等于目标硬件验证。

#### 3. 当前更合适的实现取舍

“更合适”依据是减少重复开发、缩短首个物理闭环路径、保留可验证替换边界；这是工程建议，不是基准测试结论。

1. 优先查清 SDK 实际支持什么，再从架构 7.2 分支选后端。已有轨迹/保持能力可复用；只有位置写入时再补缺失的周期执行。
2. 保留统一执行入口、真实反馈、停止/保持、时间与动作来源，这些不能因简化而删除。首期资源固定、本地管理即可。
3. 固定任务先用显式状态机；算法复用 MoveIt/Nav2 等现有候选，灵巧手先验证一个可复现抓型。硬件配置决定能否增加连续 IK、复杂抓型或模型策略。
4. 数据和 VLA 骨架早建，模型接管在执行/数据链验证之后；不把 VLA 移出一期，也不以训练模型掩盖接口问题。
5. 下一份有价值的证据是具名命令/反馈样例和能力表，然后是模拟后端故障用例；继续增加总体图不能替代这些交付。

#### 4. 冗余的处理原则

真正冗余的是相同规范的多份正文，而不是不同粒度的图。保留用户要求的图片、总体图、七类职责展开图；删除其中重复接口表/长篇规则，链接到主定义。保留 Galbot 来源、历史决策和逐日过程，不通过删除历史掩盖建议变化。

当前信息归属见 [README](../README.md)。旧记录的版本名表示当时状态，链接通常指向现行文档，不是不可变版本快照；不能用历史标题推断当前内容仍是 v0.1/v0.2。

#### 5. 验证与剩余工作

本轮检查本地链接、围栏、图节点引用、当前版本引用和前后文本规模；不是 Mermaid 渲染验证或运行测试。所有本轮技术修正均需在模拟/真实执行中验收，特别是正常终点、重定时、取消晚到、停止包络和持物资源移交。

仍缺机器人接口、Orin 版本/资源、手部能力、真实反馈与物理判据。这些决定最终实现选择，不能在文档里填成已验证事实。

#### 6. 文档规模对比

口径：下列 8 份当前入口/规范/图示文档的 UTF-8 解码字符数（含 Markdown 和空白），不等于重复率；新增审查报告、决策增补与历史记录不在本表分母内。执行契约因补齐缺口而增加，其余主要压缩重复定义。

| 文档 | 修订前字符数 | 修订后字符数 |
|---|---:|---:|
| README.md | 1,159 | 874 |
| 项目上下文.md | 2,785 | 1,376 |
| 当前进度.md | 1,923 | 720 |
| 软件架构设计.md | 16,880 | 13,616 |
| 动作执行契约.md | 5,747 | 6,997 |
| 一期建设计划与开源选型.md | 11,499 | 10,010 |
| 系统架构图.md | 7,160 | 6,236 |
| 各层架构图.md | 8,105 | 5,105 |
| 合计 | 55,258 | 44,934 |

当前工作文档净减少 **10,324 字符（18.7%）**。图片、七类职责图及既有历史保留；不是按长度判定工程完整性。

## 06 — 历史代码盘点与快速复用评估

- 目标：检查用户新提供的 `bindu/历史代码/`，梳理已有功能、可快速复用模块及缺口。
- 变更：新增[盘点与复用报告](../历史代码盘点与复用评估.md)、[源码索引/脚本/验证附件](附件/2026-09-16-历史代码盘点/盘点统计.json)；更新入口、上下文、进度、计划与架构的实现资产说明，记录 D013。原始源码/ZIP 保留不改，旧过程记录不覆盖。
- 发现：已展开源码 1,337 个（含厂商/第三方/重复文件），Python 304 个；17 个 ZIP 格式归档。内层 `ros2_ws.zip` 另有模型、OCS2、ros2_control 和 LeRobot 资产；ROS 1/2 混用、缺依赖、x86-64 库、采集手部1维等阻塞明确列出。
- 验证：304 个 Python AST 无语法错误；4 个原有纯协议测试通过；标准 C++ 单独编译插值器及原仿真，6 个场景通过；提取原函数离线复现旧执行器补发/过早完成；AST 定位保存函数未定义变量。首次审查脚本因本机 Python 3.9 对类型注解求值失败，补齐被提取源码的 future annotations 后完成验证；该失败属于审查工具，不归因历史源码。
- 结果：可优先复用插值内核、协议/调试工具、模型、位姿转换/手型模板；采集/VLA/规划有基础但需适配。原代码与 README 不一致的地方以实现与证据分级。尚无当前 Orin/机器人部署测试，未启动历史脚本或远端服务。
- 收尾：复核1,337个已索引源码文件哈希，无变动；检查8份更新文档的本地链接与代码围栏，无缺失；版本对照结果保存到附件统计。双份腰腿工程按排除缓存的统一口径为59个文件一致。
- 下一步：依据报告第五节，在独立开发区提取单臂单手最小链；核对当前机器与历史模型/SDK的对应，先修执行合同与数据语义，再进入目标硬件验证。原有导航学生分线保留。

## 07 — 补充代码盘点与分类去重精选包

- 目标：补充检查 `历史代码/代码/`，依据用户要求将有参考价值代码分类整理为一个文件，去除重复并保留差异。
- 发现：7个补充归档含873个源码成员，其中124个与首批已有内容一致，654个 Python AST 检查通过；新确认连续 IK、VR/zmq_tools、ROS 1 导航、YOLO/UNet/FoundationStereo 与 ACT。清单表只作为线索；配置引用的 FoundationStereo 权重缺失，不能据表中说明视作齐备。
- 变更：扩展[盘点报告](../历史代码盘点与复用评估.md)第七/八节；生成[精选 ZIP](../代码参考包/Bindu-历史代码精选-2026-09-16.zip)及说明、来源指纹、去重映射、目录恢复脚本；更新上下文、进度、README、计划及 D014。
- 整理结果：10类、1,961个所选路径、1,509份独立文件内容、452份重复不再存副本；不同版本不模糊合并。4个路径的字面量凭据在副本中脱敏；源码算法未修复。过滤日志/缓存/视频/权重/mesh/库/私钥及完整 googletest，保留所选模块已有许可。补入早期 EtherCAT/Qt 接口参考。
- 验证：ZIP CRC/路径检查通过；恢复1,961个所选路径后逐项 SHA-256 一致；525个独立 Python 文件 AST 无语法错误。抽取所涉及展开源文件前后哈希一致、读取归档大小与修改时间一致，原始目录未修改。初轮过滤发现大写 `Log`，已按不区分大小写修正并重新生成；凭据候选复核仅余算法 `cls_token` 变量，未作为密钥修改。验证脚本一次编码别名拼写错误已纠正，不是历史代码缺陷。
- 证据：补充盘点[脚本/统计](附件/2026-09-16-历史代码补充盘点/补充盘点统计.json)；精选包[构建脚本](附件/2026-09-16-代码参考包/build_reference_package.py)与[验证结果](附件/2026-09-16-代码参考包/整理验证结果.json)。运行命令 `python3 bindu/过程记录/附件/2026-09-16-代码参考包/build_reference_package.py`。ZIP SHA-256：`86ebf6513cb65a06cac5b7f8072c96e7f79ecf21ba3b30dd0ff8dfc2895f61e3`。
- 边界与下一步：没有运行历史控制、模型、导航、启动或改时钟脚本；未声称可直接上机。下一步从精选包接入单臂单手最小工程，修复执行/采集语义并完成模拟验证，再核对 Orin 与真实硬件。

- 最终收尾：首批1,337个展开源码文件全部重新核对，指纹无变化；8份更新文档本地链接通过，ZIP 排除项检查与 CRC 再次通过。结果见[收尾检查](附件/2026-09-16-代码参考包/收尾检查.json)。

## 08 — 补充 GUI 复用入口与来源核实

- 目标：回应用户对 `legs_Control_second` 中 GUI 设计参考价值的提醒。
- 核实：本地实际目录 `legs_control_second/legs_necks_control` 仅找到 GUI 点对点输入设计约定；完整 Qt 5 Widgets 实现在第一版 `legs_Control_first/examples/qt-exipc`。第二版设计和第一版界面代码原已收录；本轮没有虚构第二版 GUI 工程。
- 变更：新增[GUI 导读](../代码参考包/GUI参考导读.md)，列出标签页、关节反馈、目标/模式、头部与底盘调试交互；标注 ROS 1/IPC/串口耦合、真实反馈与本地状态差异、GUI/执行线程边界及未打包图标。同步报告、上下文、进度，构建脚本将导读加入包内。
- 验证：重新构建 ZIP，完整性/恢复/原始源指纹通过；两个主版 `.ui` XML 可解析，所选 GUI 路径与包内指纹一致，新增导读内外一致；首批1,337个源文件无变化。源码计数仍为1,961个选中路径、1,509份独立内容、452份重复，仅新增导读元数据。未启动 GUI、Qt 构建、ROS 或硬件。
- 证据：[GUI 复核结果](附件/2026-09-16-代码参考包/GUI复核结果.json)；新版 ZIP SHA-256：`9c525d206860787011ed2d8d6b3e6e7d9659c57805bc0a7779867ff7aa8e2179`。上条记录哈希为当时版本，保留溯源。
- 下一步：GUI 布局与交互作为后续调试客户端参考，控制目标仍经公共执行接口；未指定新平台必须使用 Qt。

## 09 — 完善代理规范并集中早期参考资料

- 目标与变更：用户要求适合 GPT-6 的项目规范、减少 Markdown 增殖，并明确 Bindu 为主要工作区；原地完善根目录 [AGENTS.md](../AGENTS.md)，加入执行/工程/验证、默认0个新增 Markdown、同日合并记录和按变化更新规则，记录 D015。写法核对 [OpenAI GPT-6 指引](https://developers.openai.com/api/docs/guides/latest-model) 与 [代理配置说明](https://learn.chatgpt.com/docs/agent-configuration/agents-md)。
- 迁移：`control架构.md`、`robotwin_pi05.md`、`ydh-catering-architecture.md`、`日志与系统服务.md`、`软硬件系统架构.md` 从根目录移入 `参考资料/Galbot/`；同步当前文档链接和来源描述。
- 验证与结果：5份迁移文档前后 SHA-256 一致；当前文档本地链接检查通过，根目录仅余 AGENTS.md 一个 Markdown。本次新增 Markdown 为0，未生成额外报告或验证附件。
- 后续：日常工作在 Bindu 及既有主文档中持续推进，参考资料按需读取；本次未扩大为 Bindu 全量文档合并或删除。
- 同日补充：按用户要求在 AGENTS.md 第4节加入较大修改/bug 修复后的本地提交规则，D015 记录确认依据；已复核新增条文。`git status --short` 确认根目录尚非 Git 仓库，`bindu/.git` 不存在，本次仅补充规范，未创建仓库或执行提交。

## 10 — 收敛可扩展骨架与近期实施规划

- 目标与变更：根据用户关于 Ubuntu 先组合骨架、且必须支持后续扩展的连续补充，更新架构8.5、计划4.1.1、当前进度和上下文；D017 修订 D016 的骨架范围，保留 v3.4 优先接入。新增文件为0。
- 结果：首版包含任务/技能、观测、能力、执行、本体、记录与运行管理边界；真实算法先模拟，软件里程碑增加同技能换策略、换本体配置、独立进程替换与故障实验。
- 验证：对照既有职责、控制权和执行契约检查边界，更新文档本地链接检查通过；仅完成方案修订，未构建工作区或执行上述实验。
- 下一步：核对 Ubuntu 环境，落实强类型合同和配置装配，跑通模拟任务及替换实验，再接 v3.4 和当前本体；导航按分线推进。

## 11 — 实现 Bindu 模块化骨架并在指定 Ubuntu 开发机验证

- 目标与变更：按用户授权，本地实现后复制至 `wxb@192.168.1.116`；按后续确认统一工程根目录为 `bindu` / `/home/wxb/bindu`，三个 ROS 包承载公共合同、独立模块核心及 ROS 装配。任务流程、异步策略、导航、执行、profile、后端、场景与记录分开，配置支持实现选择；更新既有入口、上下文、架构、计划、进度及 D018，新增 Markdown 为0，凭据未写入文件。
- 环境与复现：目标机 x86_64、Ubuntu 24.04.4、ROS 2 Jazzy、Python3.12.3；在工程根执行 `colcon build --base-paths src --symlink-install`、`python3 -m unittest discover -s src/bindu_core/test -v`、`python3 tests/validate_ros.py --output artifacts/modular-verification`，前置 source 命令见[README](../README.md#工程入口)。最终49个源码/测试文件与本地 SHA-256 一致，见[环境与指纹](../artifacts/modular-verification/environment.json)。
- 验证与修复：三个包构建、16项核心测试、13个 ROS 集成场景通过，含两策略×两配置、取消、观测迟到/过期、后端/反馈/进程故障、100 Hz 在线目标和标准 launch。初轮发现反馈过期与 launch 双重 SIGINT 破坏收尾，改为异步任务等待、串行执行/记录回调及有界退出清理后通过；失败批次保留于 artifacts，仅[最终结果](../artifacts/modular-verification/results.json)作为当前验收依据。
- 边界：全部使用模拟能力，无真实驱动/模型/传感器/Orin 实测、无硬实时或真实自适应插值结论。反馈丢失时停止未确认；记录摘要仅证明收到的数据写盘完成，记录器进程退出不伪造完整摘要。测试进程均已停止。项目尚无 Git 仓库，未执行 commit。
- 下一步：在该基线封装 v3.4 连续 IK / 遥操作输入，先连模拟执行；核对当前本体模型、具名反馈、停止与手部能力后再做真机适配，导航学生继续独立能力建设。

## 文档整理与职责归并

- 目标与变更：按用户要求检查本地文档；各层架构图并入系统架构图册第4节，独立审查报告全文并入当日第05项“审查详情”，更新入口和引用；本任务新增 Markdown 为0，归并后减少2份文件。
- 保留与去重：10张 Mermaid 图内容保留，审查发现、官方来源、统计与验证边界完整迁移；架构、执行契约、计划、盘点和 Galbot 来源对照仍独立维护，历史源码、参考原文、图片及精选包不改。进度页历史明细改为主文档入口，修正架构首页“尚未实现”的过期表述。
- 版本管理：整理期间另一任务在 `bindu/` 建立正式仓库并迁入 AGENTS.md，本任务保留这些改动；外层临时文档基线已移至 `/tmp/bindu-docs-baseline-824da25.git`，正式交付使用 `bindu/` 仓库。
- 验证：16份项目文档的180个本地链接、26个章节锚点及围栏检查通过；10张 Mermaid 图逐段与整理前原文一致，审查报告正文全文一致，旧日过程记录未改；Git 差异无空白错误。未重新渲染图示，未运行代码或机器人测试。

## 12 — 拆分设备接入、说明历史代码落点并初始化 Git

- 目标与变更：按用户要求将宽泛后端拆成关节/手/底盘驱动、命令路由、反馈汇总和薄装配；移除旧单体实现与配置入口。补充各设备采样时间和停止失败来源，保持插值属于执行层。用户追问的历史资产接入位置、骨架缺口和语音任务入口已并入现有架构文档，记录 D019，未生成新主题 Markdown。进一步按实验室能力在 `src/capabilities/` 建立规划/VLA/感知/导航独立模拟包，IK/交互/遥操作仅预留目录，公共核心不反向依赖算法包。
- 验证：设备拆分后本地测试通过；按用户后续要求将算法分包，目标 `/home/wxb/bindu` 七个包重建、20项核心测试＋1项跨包能力契约测试、15个ROS集成场景通过，新增手部单独故障与驱动替换/反馈汇总测试；80个工程文件 SHA-256 一致，节点日志无非预期异常。命令沿用README，集成输出参数为 `--output artifacts/capability-verification`，见[本轮结果](../artifacts/capability-verification/results.json)。仍全部为模拟，未操作真机。
- 版本管理：在 `bindu/.git` 初始化 `main`，排除历史归档、附件、构建/记录产物和凭据；AGENTS正文迁入仓库，父目录仅保留链接。检查通过后提交本轮工程代码与相关文档，保留并行文档任务已产生的提交，不配置或推送远端；未配置用户作者时使用一次性的 Codex 自动化身份，不修改全局 Git 配置。
- 文档协作：保留工作期间已有的架构图/审查记录合并整理结果，不恢复被合并的独立文件；仅在现有文档补本轮职责和进度。下一步仍为 v3.4 输入与IK适配，语音及真实感知/导航按已定义边界后续实现。

## 13 — 重新划分 core 职责并按已有模块分类目录

- 目标/变更：回应 core 含过多设备后端实现的问题，移除旧包，分成公共契约、任务、执行、硬件接入、记录五包；共享消息、能力包和运行装配按职责分类，共11包。执行只依赖 `ExecutionIO`；同步导入、provider 配置、包依赖及测试路径，保持 ROS 入口/动作行为，新增 Markdown 为0。
- 验证：本地22项测试通过；目标开发机在 `build/reclassified`、`install/reclassified` 独立构建11包，22项模块/依赖边界测试、15个模拟集成场景通过。复现命令见[README](../README.md#工程入口)，结果见[本批证据](../artifacts/reclassified-verification/results.json)；旧 core 不可导入，模拟节点均已退出。99份工程文件两机指纹一致；回归后仅清理2份 manifest 和1份测试文件尾部空白，原始/最终指纹均保留。
- 评估：此前是文件拆开而包过宽；本次形成可维护的依赖边界，但 ROS 适配仍集中运行包、硬件各端点仍在同包内以子模块替换、任务端口仍有动态调用。没有实现新增算法、生产插值或真实驱动，也不把目录独立等同于进程/部署独立。
- 记录与下一步：原地更新入口、架构、上下文和进度，D020 更新旧包组织；保留历史资产及其他未跟踪文件。检查后本地提交。后续仍以 v3.4 IK/遥操作接入模拟执行为先，按实际依赖再拆算法实现包。

## 14 — 确认历史 VLA 客户端与远端推理范围

- 目标/事实：用户确认机器人本地只需要 VLA 客户端；复核历史 `pi_inference.py` 的观测发布/动作接收、`vla_infer_sync.py` 的按需观测回复及 `state_producer.py` 的三路图像/关节封装，已有可复用实现。
- 变更：D021 明确范围，原地修正上下文、架构/图、计划、盘点与进度中的部署和复用表述，新增 Markdown 为0；区分历史已有客户端与新骨架尚未迁入。
- 验证/下一步：源码阅读、文档链接与差异检查通过；未运行历史脚本、连接模型服务或复跑未修改代码的 ROS 测试。后续按已有协议提取客户端，补 ROS 2、灵巧手语义及统一执行接入，先以模拟服务验证。
