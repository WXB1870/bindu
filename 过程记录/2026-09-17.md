# 2026-09-17 过程记录

## 01 — Pi 双模式模拟复测与启动就绪修正

- 目标/基线：按用户“模拟测试一下”运行指定 Ubuntu 开发机上的 Pi 客户端测试，开始时源码为 `a03c1bb`，110个源码/测试文件两机一致；使用回环 Pi 服务、三路合成 RGB 与模拟关节，未连接真实模型或机器人。
- 发现/修正：首轮28项模块测试通过，13个 Pi 场景中11个通过、2个在目标接纳时被拒绝，见[首轮结果](../artifacts/pi-simulation-2026-09-17/results.json)。原测试以自身发布次数代替客户端就绪；旧拒绝未记录具体原因，无法还原每次缺失条件。新增 `vla/pi/ready` 标准 Trigger 服务、`PI_GOAL_REJECTED` 事件/日志，测试先查询实际就绪再提交；新增延迟观测场景确定性验证未就绪拒绝及后续接纳，最终日志拒绝原因为 `PI_OBSERVATION_NOT_READY`，新鲜度限制未放宽。
- 验证/证据：11包重建、28项模块测试、14个 Pi 集成场景全部通过，最终110个工程文件指纹一致，见[最终结果](../artifacts/pi-simulation-2026-09-17-final/results.json)及[环境指纹](../artifacts/pi-simulation-2026-09-17-final/environment.json)。接纳计数与参考消息一致，停止后参考清空且控制权可重新获取；13个正常记录摘要完整且无丢弃，故意杀死的记录器不生成完成摘要，日志无非预期异常，测试进程全部退出。
- 复现/范围：source ROS Jazzy、`install/pi/setup.bash` 并添加 `artifacts/pi-deps` 到 PYTHONPATH 后，执行 `python3 -m unittest discover -s tests -p 'test_*.py' -v` 和 `python3 tests/validate_pi.py --output artifacts/pi-simulation-2026-09-17-final`；完整环境见[README](../README.md#pi-客户端运行入口)。原有15个 ROS 场景本轮未重跑，9月16日结果保留。更新现有入口/架构/进度，仅新增当日唯一过程文件；检查后提交本次修正。下一步仍为实际 Pi 服务地址、相机同步及机器人映射核对，不将模拟会话视为真实抓取验收。

## 02 — GitHub 远端接入准备

- 目标/变更：用户提供 `https://github.com/WXB1870/bindu`。本地配置 HTTPS origin，将远端初始提交 `b782c9a` 与本地 `fd7d400` 合并，保留 MIT LICENSE；README 保留现有工程说明并吸收远端项目简介，增加同步入口。未纳入未跟踪的参考包和图片，也未上传历史资产或测试数据。
- 检查：GitHub API 确认为公开仓库、默认分支 main；Ubuntu 可读远端。本机 HTTPS 读取超时，凭据检查仅返回可用性，不输出凭据，当前无可用推送认证。借助 Ubuntu 临时 clone 和 Git bundle 获取初始历史，未强推或改写远端。
- 保全：发现 Ubuntu `/home/wxb/bindu` 已有独立提交 `0846680` 及未提交源码变更。将 `.git`、`.gitignore`、`src`、`tests`、`tools`、二进制差异与110文件 SHA256 清单备份至 `/home/wxb/bindu-backups/before-github-20260917-111928`；构建目录、依赖和实验数据保留原位，尚未更换服务器 Git 元数据。
- 结果/后续核验：用户完成 GitHub Desktop 登录和仓库添加后，`git ls-remote origin refs/heads/main` 返回 `27d7fa5adfc7a0ce5f663547b0faf72a749380fd`，与本地 HEAD 一致，首次推送已完成。命令行推送仍报无法取得密码，Desktop 已登录，后续本轮记录通过 Desktop 同步。下一步将 Ubuntu 工作区接入同一历史并验证快进拉取；服务器迁移尚未执行。本次未修改生产源码，不重复 Pi 场景测试。
