# 决策记录

状态约定：**已确认**为用户明确要求；**建议**为待确认/验证方案；**已验证**必须附实施证据；**已替代**保留历史并链接替代项。

## D001 — 硬件无关复用，首期轮式、后续双足

- 日期：2026-09-15；状态：已确认。
- 依据：用户强调复用不依赖具体硬件，支持轮式与双足；随后明确首期轮式优先。
- 影响：任务与技能公共接口独立于关节布局及厂商 SDK；首期不展开双足步态和平衡实现。模型跨本体泛化不视作接口复用的自动结果。

## D002 — ROS 2 通信与位置执行边界

- 日期：2026-09-15；状态：已确认。
- 依据：用户选择 ROS 2；接口为底盘速度、关节 CSP 位置，部分关节可读力矩。
- 影响：按能力适配移动和关节后端；100 Hz 来源包含规控与 VLA，统一管理时序和执行，实际下发频率按硬件确定。
- 后续澄清：此条记录接口需求，不代表底盘已接通；用户后称“只有关节级位置接口”，当前可用性以 D007 和项目上下文为准。

## D003 — 一期建设全部基础技能

- 日期：2026-09-15；状态：已确认。
- 依据：用户明确语音、导航、感知、规划均未具备；目标为语音指定饮料、导航抓取、持物返回。
- 影响：计划必须包含部署、模型/标定资产、机器人适配及技能验收；VLA 客户端、示教管线和日志保留在一期范围。

## D004 — 开源组件和部署基线

- 日期：2026-09-15；状态：建议，未实测。
- 依据：已确认 Orin、Ubuntu 22、ROS 2；结合官方文档推荐兼容组合与已有算法库。
- 方案：优先 Humble；Nav2、FunASR、YOLO、Isaac ROS FoundationPose、MoveIt 2 等，详情和来源见[选型计划](一期建设计划与开源选型.md)。
- 影响：部署前核实 JetPack、ROS 版本、驱动、模型环境；ros2_control/JTC/Ruckig 作为执行基础，在线目标管理仍需自建。具体型号、版本与效果不视作已确认事实。
- 后续修订：D012 将执行组件改为按硬件能力择一适配，行为树不再默认前置；本条原组合建议保留为历史。

## D005 — 固定场景先打通传统策略闭环

- 日期：2026-09-15；状态：建议。
- 依据：一期目标为简单移动抓取，基础技能尚未建设。
- 方案：建议固定取物区域、返回起点、返回后保持持物；传统策略先串联任务，后续接入可切换 VLA 策略。
- 待确认：地点和交付语义；用户已确认可使用不同策略，但尚未确认以上具体流程假设。

## D006 — 文档集中与持续记录

- 日期：2026-09-15；状态：已确认。
- 依据：用户要求计划放在 `bindu/`，长期维护上下文和过程记录。
- 落实：计划迁入 `bindu/`；建立入口、上下文、进度、决策与按日过程记录；根目录 `AGENTS.md` 指定后续读取和更新规则。
- 影响：每次实质进展更新当前状态，并追加过程记录；历史记录不被当前状态覆盖。

## D007 — 基于已有底层控制调整启动顺序

- 日期：2026-09-15；状态：现状已确认，实施顺序为建议。
- 用户确认：机器人底层控制已做好，只有关节级位置接口；一名学生正在学习导航。
- 需澄清：是否可读取真实关节角/故障，以及此前提到的底盘速度接口是否已开放；不推断底盘接口已取消或已具备。
- 建议：主线先完成单臂模型、规划、位置执行、反馈与日志的最小闭环；导航学生分阶段完成仿真、传感器/定位输入和真机到点交付。
- 原因：规划、视觉抓取和 VLA 都依赖可靠执行接口；导航可独立推进并通过明确接口与操作线汇合。
- 影响：复用现有控制器；先检查其实际插值、缓冲和停止能力，再实现缺失的 100 Hz 执行管理。细化 P0/P1 顺序，不移除后续语音、感知、VLA 或数据范围。
- 后续调整：D009 将实施前置工作改为先明确整体架构，单臂验证作为架构之后的实现链。

## D008 — 优先审查已有 MoveIt，分开规划与在线跟踪

- 日期：2026-09-15；状态：旧代码存在由用户确认，技术路线为建议。
- 依据：用户已有旧 MoveIt，询问连续轨迹求解能力；尚未获得源码，无法归因其实际问题。
- 建议：复用整段规划、模型和碰撞配置；已知笛卡尔路径评估连续 IK/computeCartesianPath，在线更新目标评估匹配版本的 Servo；统一进入位置执行后端。
- 影响：细化 D007 的启动顺序，先审查和验证已有代码，避免重复搭建；100 Hz 采样与全局规划频率分离。VLA 关节目标不额外走 IK。
- 验证边界：已核对官方接口与 Humble Servo 文档；连续求解、控制周期和真机效果均未实测。用户确认本机没有源码；具体连续场景与历史代码实现仍待后续核对。

## D009 — 架构先行与总体分层草案

- 日期：2026-09-15；状态：架构先行由用户确认，具体方案为建议草案。
- 依据：用户要求首先把软件架构做明白，安排一整套架构。
- 方案：[软件架构设计 v0.1](软件架构设计.md)，明确任务/技能/策略/动作处理/执行/本体后端，及观测、运行监督、数据/日志公共能力。
- 关键建议：所有应用动作源经过统一控制权；按资源与代次管理执行；VLA/规划到 100 Hz 与 100 Hz 到硬件周期分工；ROS 包、逻辑模块与进程边界分别设计。
- 影响：后续顺序变为总体边界 → 字段级契约 → 执行时序 → 技能/部署合同 → 模拟闭环 → 原 P0–P4。细化 D007/D008 的实施顺序，继续保留旧代码复用与导航分工。
- 后续更新：架构先行仍有效；末端假设及 P0–P4 实施顺序由 D011 修订，历史取舍保留。
- 验证边界：已对照参考文档并核对官方 Servo、ros2_control 与 ROS QoS 资料；仅完成草案及文档一致性检查，未生成消息/节点，未部署或测试真机。

## D010 — 对照 Galbot 细化生命周期与执行提交

- 日期：2026-09-15；状态：用户要求再优化，具体 v0.2 设计为建议，未实测。
- 依据：用户追问是否参考 Galbot 并要求优化；重新对照软硬件文档 §11/§14/§17、YDH §5/§6/§11/§12/§15–§17、日志文档的就绪/记录说明。
- 来源修正：`control架构.md` 是设计建议，不能统一当作 Galbot 的实装事实。新增[逐项对照](Galbot设计对照.md)明确沿用、适配和新增。
- 变更：Context/Attempt/Runtime 与 HeldObjectSession 生命周期；候选准备和本地唯一时间轴提交；区分未来段和即时目标；取消后停止/保持/recovery 权限；配置快照、模式 readiness、多维结果与日志证据。
- 影响：A1/A2 有[执行契约草案](动作执行契约.md)，首期自研围绕少量入口装配模块；v0.2 取代 v0.1 当前设计，保留 D009 历史。具体 ROS schema、硬件周期/反馈和保持能力仍待定稿。
- 验证：文档来源对照与本地链接/结构检查；没有执行代码测试或机器人实验。
- 后续修订：D012 的架构 v0.3 / 契约 v0.2 为当前建议，补齐正常结束、重定时与保持资源移交；本条 v0.2 为历史版本说明。

## D011 — 全部使用灵巧手，提前臂手示教与抓取验证

- 日期：2026-09-15；状态：灵巧手为用户确认；开发顺序与技术细节为建议，未部署或实测。
- 依据：用户补充“我都会用灵巧手”，询问开发顺序。手部协议、自由度/耦合、反馈与现有遥操作的手指能力仍未知。
- 变更：当前架构/执行契约将末端明确为灵巧手，补充 HandProfile、命名动作与驱动耦合、跨后端时序/部分失败、分阶段抓取与持续保持责任；不默认手部具备 CSP 同周期或触觉/力控。
- 顺序：按[计划第四节](一期建设计划与开源选型.md)的 P0–P6，先最小架构/模拟闭环、单臂单手执行，再遥操作示教、已知位置自动抓取、视觉、移动语音任务、VLA 策略验收。日志与数据、VLA 客户端骨架提前；导航并行。
- 影响：替代旧 P0–P4 中将示教集中在移动任务之后的安排，保留 D009 架构先行和 D010 所有权/时间轴设计；Galbot 原文及既有过程记录保留其原本末端描述。
- 候选：需要人体手姿映射时评估 dex-retargeting，核对其官方 README 与关节顺序说明；不是已验证的手部驱动或抓取控制器。
- 验证边界：本次只做依赖关系、文档链接与语义一致性检查；尚无手型模型、消息实现、数据采集或机器人测试结果。

## D012 — 审查后收敛首期实现与文档职责

- 日期：2026-09-16；状态：用户要求审查；技术取舍为修订建议，尚未实测或获用户逐项确认。
- 依据：[审查记录](过程记录/2026-09-16-架构与文档审查.md)，现有 SDK/周期/反馈未知，完整平台角色不宜直接等同首期开发量。
- 保留：ROS 2、轮式优先/双足扩展、灵巧手、统一执行、日志与数据、一期 VLA 范围。
- 修订：现有控制器优先；按能力选择 SDK 适配或 ros2_control 基线，JTC/Ruckig 不默认叠加。固定任务先用显式状态机，行为树/RTC/复杂并发按需引入；固定资源表仍保留代次、取消与保持责任。
- 正确性：正常有限轨迹终点与断流分开；改变执行时间需更新唯一时间轴；停止也受路径/接触约束；持物监测与臂资源写权限分开移交；受限试抬和运输使用不同判据，试抬前更新规划几何假设。
- 文档：架构 v0.3 管职责与装配，契约 v0.2 管执行语义；上下文仅事实，进度仅状态，计划仅候选/阶段/验收。A0–A4 并入 P0；图示仅辅助，过程记录保留。
- 验证：官方控制器/版本文档核对、来源抽查、文档结构/链接及去重统计；未部署、未证明性能最优。

## D013 — 历史代码成为实现资产，按模块复用

- 日期：2026-09-16；状态：用户提供代码并要求盘点；源码事实已检查，复用顺序为建议。
- 依据：[历史代码盘点与复用评估](历史代码盘点与复用评估.md)及其索引/验证结果。此前“本机无源码”的整体假设已不适用；D008 所指完整旧 MoveIt 工程仍未找到。
- 新事实：存在 C++ 插值器、cuRobo ROS 2 适配、任务框架、ROS 1 臂手/VLA/采集；内层 ZIP 还有 OCS2、ros2_control、模型及 LeRobot 插件。C++ 6 个原有场景与协议4个原有测试通过，不能算整机验收。
- 建议：保留原始资产和版本指纹，先提取插值内核、协议与必要模型，再接当前已有控制器。规划从 cuRobo/OCS2 与 MoveIt 候选中验证选取，不默认同时建设多套执行链；补齐执行时序、真到达、采集保存及多指语义后再部署。
- 影响：更新计划的实现起点，不替代 D012 的统一合同和单后端原则，不改变用户首期范围，不把历史多机地址/模型/演示任务当作当前设备事实。
- 边界：未修改历史源码、未移动/删除归档、未启机器人或网络服务；SDK ARM64、当前 Orin 环境和缺失依赖待核实。

## D014 — 历史代码分类精选，精确去重并保留差异版本

- 日期：2026-09-16；状态：分类去重由用户明确要求，整理产物已生成；可部署性未验证。
- 依据：用户补充 `历史代码/代码/` 后，要求把可用、有参考价值的部分单独整理为一个文件。
- 实施：交付单一 ZIP，10类目录、复用分级、来源指纹和目录恢复脚本；仅按完整文件内容去重，行为/配置不同版本保留。过滤缓存、日志、二进制、权重与私钥等，所选代码中识别出的字面量凭据仅在副本脱敏。原始资产不改。
- 补充事实：找到连续 IK/VR、ROS 1 导航、软管 YOLO/FoundationStereo 与 ACT；更新此前“未找到导航/YOLO源码”的范围判断。它们仍需迁移、依赖补齐和当前本体验收，不替代 D012 的统一执行合同。
- 证据与边界：[参考包说明](代码参考包/README.md)、[整理验证结果](过程记录/附件/2026-09-16-代码参考包/整理验证结果.json)。精确去重、恢复与静态检查已做，未执行历史控制/启动/时间同步脚本、未部署服务或控制机器人。

## D015 — 以 Bindu 为主工作区，限制文档增长

- 日期：2026-09-16；状态：用户已确认工作重心、要求限制文件生成并迁移早期 Galbot Markdown；具体规则已写入 [AGENTS.md](../AGENTS.md)。
- 规则：完善代理执行、工程、验证和收尾要求；默认每项任务新增0个 Markdown，优先原地更新，仅必要独立主题允许有限新增；一天只维护一份过程记录，按实际变化更新主文档。
- 组织：5份早期 Markdown 原文迁入 `参考资料/Galbot/`，根目录保留代理规范；`bindu/` 继续承载主项目工作。参考资料仅按需读取，不作为当前实现事实。
- 影响：细化 D006/D012 的长期记录职责，限制独立总结/导读/审查报告的增殖；本次没有新增 Markdown、没有合并或删除现有 Bindu 文档。
- 同日补充（用户明确确认）：较大修改或 bug 修复后执行本地 Git 提交；完整执行规则见 [AGENTS.md 第4节](../AGENTS.md#4-验证与交付标准)。检查时项目根目录不是 Git 仓库，`bindu/` 也没有 `.git`，后续实际提交前需具备对应工作仓库。

## D016 — 近期以 v3.4 遥操作验证统一执行与数据链

- 日期：2026-09-16；状态：基于用户要求提出的最新规划建议，未实测。
- 依据：v3.4 已有 VR、连续 IK、模式管理与通信；底层控制已具备，当前缺口主要是模型/接口核对、统一执行和可用数据。
- 方案：执行基础先行，将 v3.4 接入公共执行入口，先单臂单手遥操作抓取及 episode；GUI 做最小调试入口，导航学生独立推进，VLA 客户端骨架提前验证。详见[计划4.1.1](一期建设计划与开源选型.md#411-当前实现批次以-v34-遥操作验证首条执行链)。
- 影响：细化 D013 的资产优先级与 P0–P2 实现批次，不改变 P0–P6 出口、ROS 2、单一执行所有权或一期完整目标，不要求同步搭建多套规划执行框架。

- 同日补充：用户提出先在 Ubuntu 组合中间骨架，再逐个接入能力；采纳为实施建议，前置单机 ROS 2 模拟执行闭环。v3.4 仍是首条实际策略接入线，先连模拟后端再连真机；在线目标、定时轨迹、停止/保持分模式处理，不把自适应插值设为所有指令的必经步骤。开发机环境未核实，未开始部署。

## D017 — 用能力替换验证 Ubuntu 骨架的扩展性

- 日期：2026-09-16；状态：用户明确要求骨架支持后续扩展；具体8.5装配与测试方案为建议，未实现。
- 修订：替代 D016 补充中仅以执行链描述骨架范围的表述；建立任务/技能、观测/场景、能力适配、执行、本体、记录和运行管理边界，算法可先模拟。D016 的 v3.4 优先接入和分阶段真机验收继续有效。
- 验收：模拟任务流程之外，验证同技能换策略、换具名本体配置、独立进程实现替换及超时/故障传播；“接口存在”或“节点能启动”不等于可扩展性通过。详见[架构8.5](软件架构设计.md#85-ubuntu-上的可扩展骨架首期落地边界)。
- 影响：基础合同和可配置装配提前，真实算法逐项接入；不前置动态插件平台、复杂分布式调度或双足动力学开发，不改变统一控制权及按模式插值的执行合同。

## D018 — Bindu 工程落地，模块核心与 ROS 装配分离

- 日期：2026-09-16；状态：本地实现、复制到指定开发机验证、总工程名 `bindu` 及模块独立组织为用户明确要求；以下代码组织已实现并完成首轮模拟验证。
- 组织：`bindu/src/` 下三个职责包，包内独立 `launch/` / `config/` / `test/`，跨包测试位于根 `tests/`，生成物位于 `build/` / `install/` / `log/` / `artifacts/`。去掉临时 `ws/` 层；目标目录统一 `/home/wxb/bindu`。历史参考不混入源码，构建显式限定 `--base-paths src`。
- 技术取舍：先用无 ROS 核心与 Python ROS 适配验证合同；任务流程、异步策略、导航、执行、profile、模拟后端、场景和写盘分别维护，配置选择 provider。这不是生产实时控制实现，也不替代后续 C++ 插值/SDK 适配。原单文件 model/providers 已拆分，不保留重复实现。
- 依据与影响：D017 的替换实验现已有代码与证据；开发机实测是 Ubuntu 24.04 / Jazzy / x86_64，与 Orin Ubuntu22 不同，目标平台验证保留。当前仅串行控制资源和模拟持物，不将全架构合同视为全部实现。
- 证据：[当前进度](当前进度.md)及[实现与替换边界](软件架构设计.md#854-当前代码组织与替换边界)。修复联调中的阻塞式任务等待/回调竞争及 launch 重复 SIGINT 中断记录收尾；最终16项核心测试、13个集成场景通过，未操作真实机器人。
