# Bindu 系统架构图

日期：2026-09-16。基于[软件架构 v0.3](软件架构设计.md)、[动作执行契约](动作执行契约.md)与灵巧手补充绘制。状态：建议架构的可视化，非已实现系统；开源项目名沿用[建设计划](一期建设计划与开源选型.md)的候选，不表示已经部署。

本图册集中维护总体分层、模块关系、执行链和[七类职责展开](#4-各层职责展开)，各视图共用架构与执行契约中的定义。

## 0. 总体分层架构

科研汇报用静态图片：[总体架构图 PNG](图片/总体架构图-科研版-v1.png)；[生成记录](图片/总体架构图-科研版-v1-提示词.md)。

本图以“硬件 → 驱动适配 → 控制执行 → 算法能力 → 任务技能 → 应用”六层概括整个系统，公共平台贯穿软件各层。它是同一架构的高层视图，不替代后文模块关系图，也不沿用细节图的七个职责编号。

```mermaid
flowchart LR
    subgraph STACK[机器人系统主架构]
        direction TB
        APP[应用与交互层<br/>语音指令 · 操作界面 · 遥操作入口<br/>移动取饮料等应用]
        TASK[任务与技能层<br/>任务编排 · 导航 / 抓取 / 持物技能<br/>结果验收 · 失败恢复]
        CAP[算法与能力层<br/>感知与场景 · 定位导航 · 运动规划 / IK<br/>灵巧手抓取策略 · VLA · 遥操作映射]
        CTRL[控制与执行层<br/>统一控制权 · 动作时间轴 · 100 Hz 参考<br/>自适应插值 / 平滑 · 停止与持物保持]
        DRIVER[硬件抽象与驱动层<br/>统一设备接口 · 本体 / 灵巧手适配<br/>传感器驱动 · 厂商 SDK · 状态反馈]
        HW[硬件层<br/>轮式本体 · 手臂 · 灵巧手 · 已有控制器<br/>雷达 · RGB-D · 力传感器 · Jetson Orin<br/>后续扩展双足本体]
        APP <--> TASK
        TASK <--> CAP
        CAP <--> CTRL
        CTRL <--> DRIVER
        DRIVER <--> HW
    end
    subgraph PLATFORM[贯穿软件各层的公共平台]
        direction TB
        ROS[通信与数据契约<br/>ROS 2 · Topic / Service / Action<br/>时间戳 · 坐标系 · 消息语义]
        OPS[配置与运行管理<br/>机器人模型 · 标定 · 能力配置<br/>生命周期 · 健康 · 版本管理]
        DATA[日志与数据平台<br/>日志诊断 · 示教采集 · 回放<br/>数据处理 · 训练 / 评估]
    end
    PLATFORM -.-> STACK
    classDef application fill:#e9e5f8,stroke:#8c79ae,color:#382e50
    classDef task fill:#e6edff,stroke:#728abe,color:#223458
    classDef capability fill:#e0f2ec,stroke:#579b83,color:#244b3c
    classDef control fill:#fff0d8,stroke:#bb904d,color:#594019
    classDef hardware fill:#edf0f4,stroke:#8491a0,color:#303c4a
    classDef platform fill:#f4f5f8,stroke:#9aa4b3,color:#374154
    class APP application
    class TASK task
    class CAP capability
    class CTRL control
    class DRIVER,HW hardware
    class ROS,OPS,DATA platform
```

读图说明：

- 上层表达任务与目标，下层落实运动与设备访问，真实状态向上反馈。箭头表示层次依赖与反馈，不要求感知数据绕经执行层逐级转发；观测可从驱动直接提供给算法和验收。
- ROS 2 是软件模块通信基础，不是硬件控制总线，也不是高频循环必须穿过的中心服务。
- 硬件抽象层通过统一接口承接不同本体；控制、算法和技能仍会依据能力/约束配置进行适配。双足后续还涉及稳定性与全身协调。
- 100 Hz 为上层关节逻辑参考；底盘速度独立处理，手臂与灵巧手的实际周期、反馈和插值能力分别核实。底层关节控制由已有控制器承担。
- 本图概括建设目标，不表示所有模块已经具备；实现与验证见[当前进度](当前进度.md)，已有条件和未知输入见[项目上下文](项目上下文.md)。

## 1. 系统总览

实线表示主要调用或数据流；虚线表示公共支撑、旁路记录或后续扩展。箭头是逻辑关系，不限定 ROS 节点、进程或机器数量。图中省略部分返回箭头；技能终态必须基于真实状态和结果验收。

```mermaid
flowchart TB
    subgraph TASK[任务与技能层]
        UI[语音 / 文本指令] --> INTENT[语音识别与意图解析]
        INTENT --> T[任务编排与技能运行时<br/>导航 · 观察 · 抓取 · 持物返回<br/>验收 · 取消 · 有界恢复]
    end

    subgraph OBS[观测与环境层]
        SENSOR[3D 雷达 · RGB-D · 内置 IMU<br/>力传感器 · 可用本体反馈] --> O[状态历史与观测组装<br/>时间对齐 · TF · 有效性]
        O --> P[视觉感知<br/>YOLO · FoundationPose]
        P --> SCENE[场景与对象状态<br/>对象位姿 · 碰撞场景 · 持物事实]
        MAP[已有地图 · 站点 · 对象资产] --> SCENE
    end

    subgraph POLICY[导航与操作策略层]
        NAV[激光定位与导航<br/>Nav2 适配 · 站位]
        PLAN[传统操作策略<br/>MoveIt · 连续 IK / Servo<br/>掌心路径 · 手指抓取策略]
        VLA[VLA 客户端<br/>观测编码 · 异步请求 · 动作解码]
        TELE[遥操作适配<br/>臂动作 · 手指重定向]
    end

    T --> NAV
    T --> PLAN
    T --> VLA
    T -->|观察请求| P
    SCENE -->|目标与场景| T
    SCENE --> PLAN
    O --> NAV
    MAP --> NAV
    O --> PLAN
    O --> VLA
    O -->|物理验收依据| T
    OP[已有遥操作设备] --> TELE
    MODEL[远端 VLA 模型服务<br/>机器人本地只运行客户端] <-->|推理请求 / 响应| VLA

    subgraph EXEC[统一执行层：本地控制权与动作时间轴]
        PREP[动作与时间轴准备<br/>按需 IK · 动作块衔接 · 约束检查<br/>生成 100 Hz 逻辑关节参考]
        E[执行管理：唯一提交入口<br/>资源租约 · 代次 · 有效期 · revision<br/>停止 / 接管 · 执行反馈]
        HOLD[持续持物会话<br/>臂手保持 · 跨技能移交]
        BASE[移动执行后端<br/>速度约束 · 超时停止]
        JOINT[臂手执行后端<br/>受约束自适应插值 / 平滑<br/>按各硬件周期采样下发]
        PREP -->|候选轨迹| E
        E --> BASE
        E --> JOINT
        E <--> HOLD
    end

    PLAN --> PREP
    VLA --> PREP
    TELE --> PREP
    NAV -->|速度提议，独立合同| E
    T -->|执行许可 / 取消 / 持物移交| E
    E -->|执行结果| T
    O -->|真实状态与反馈年龄| E

    subgraph HW[硬件适配与机器人]
        BHW[轮式底盘适配<br/>速度 / odom 接口待核实]
        AHW[已有手臂 / 身体控制器<br/>关节位置 · CSP]
        HHW[灵巧手 SDK 与控制器<br/>驱动耦合 · 协议 / 周期待核实]
        BIP[后续：双足后端<br/>行走 · 支撑 / 平衡 · 全身协调]
    end

    BASE --> BHW
    JOINT --> AHW
    JOINT --> HHW
    E -.->|未来能力适配| BIP
    BHW -->|定位所需反馈| O
    AHW -->|实际反馈，能力待核实| O
    HHW -->|实际反馈，能力待核实| O

    subgraph COMMON[全系统公共支撑]
        CONFIG[RobotProfile / HandProfile<br/>模型 · 关节映射 · 限制 · 标定 · 配置版本]
        SUP[运行监督<br/>模式 · 启停 · 就绪 · 健康 · 执行许可]
    end
    CONFIG -.-> PLAN
    CONFIG -.-> PREP
    CONFIG -.-> E
    CONFIG -.-> O
    SUP -.-> T
    SUP -.-> E

    subgraph DATA[日志、示教与离线数据管线]
        REC[异步记录与诊断<br/>事件 · 原始观测 · 动作 · 实测 · 版本]
        DATASET[Episode 管理与数据处理<br/>对齐 · 质检 · 动作标签 · 数据集导出]
        TRAIN[离线训练 / 评估<br/>通过验证后发布模型版本]
        REC --> DATASET --> TRAIN
    end
    T -.-> REC
    O -.-> REC
    TELE -.-> REC
    VLA -.-> REC
    PREP -.-> REC
    E -.-> REC
    TRAIN -.->|显式版本更新| MODEL

    classDef task fill:#e8edff,stroke:#6474b9,color:#182345
    classDef sense fill:#e1f2ec,stroke:#519c85,color:#163c31
    classDef policy fill:#e5f0fc,stroke:#5a8fbd,color:#183954
    classDef execute fill:#fff0d8,stroke:#bf914d,color:#573b12
    classDef hardware fill:#eef0f3,stroke:#83909d,color:#27313b
    classDef data fill:#eee6f7,stroke:#9b7eb8,color:#443052
    classDef future fill:#fafafa,stroke:#969da5,color:#66717b,stroke-dasharray:5 5
    class UI,INTENT,T task
    class SENSOR,O,P,SCENE,MAP sense
    class NAV,PLAN,VLA,TELE,OP,MODEL policy
    class PREP,E,HOLD,BASE,JOINT execute
    class BHW,AHW,HHW,CONFIG,SUP hardware
    class REC,DATASET,TRAIN data
    class BIP future
```

图示仅表示调用/数据关系，执行状态、重定时、持物移交等规则以[执行契约](动作执行契约.md)为准；首期实现选择见[架构 7.2](软件架构设计.md)。

## 2. 执行链放大图

此图解释总览中最容易混淆的 100 Hz、插值与控制器边界。

```mermaid
flowchart LR
    PL[规划轨迹] --> A[动作适配与候选准备<br/>时间参数化 / 动作块衔接<br/>100 Hz 逻辑参考]
    VL[VLA / 遥操作动作] --> A
    A --> C[执行端原子提交<br/>权限 · 有效期 · 时间轴版本]
    C --> BUF[已接纳的参考缓冲]
    BUF --> S[按已提交曲线插值<br/>按后端周期采样]
    S --> D[硬件适配<br/>臂位置接口 / 手部 SDK]
    D --> HW[已有低层控制器]
    HW --> FB[真实反馈与健康]
    FB --> C
    C -.->|当前边界状态与 revision| A
```

- 总线周期归实际持有控制器/总线的一端；若现有控制器已完成插值，则采样/适配按其合同安排，不能叠加两个彼此未知的平滑器。
- 速度、加速度、jerk、路径偏差与接触阶段约束由具体合同定义；能力/约束不满足时拒绝或显式重定时，不能用平滑掩盖。
- 提交成功、SDK 下发成功、控制器接受、物理动作完成分别记录；缺少真实反馈时不能宣称闭环验收通过。
- 臂手共享时间与阶段语义，不默认物理同步；多个后端部分拒绝时有明确停止/保持流程。

## 3. 部署与实现

本图表示完整目标能力，不表示首期要同时部署所有模块。执行、任务、记录职责及上游组件的装配见[架构第 8/11 节](软件架构设计.md)；开发阶段见[建设计划](一期建设计划与开源选型.md)。

## 4. 各层职责展开

更新：2026-09-16。本文保留七类职责组的图示，辅助阅读[架构 v0.3](软件架构设计.md)；不是七层串行部署或另一份接口规范。执行规则以[契约 v0.2](动作执行契约.md)为准。

沿用详细视图的七个编号。第 2 组为共享观测服务，第 6、7 组贯穿系统，不表示所有请求必须逐层穿过七个节点。图中外部输入/输出节点仅标明接口，不属于该层实现。开源项目名称沿用现有候选，不新增版本或性能承诺。

### 1. 任务与技能：决定做什么、何时算完成

```mermaid
flowchart TB
    INPUT[语音 / 文本] --> PARSE[语音识别与意图解析<br/>饮料词表 · 参数校验 · 消歧]
    PARSE --> TASK[TaskRuntime<br/>任务编排 · TaskContext · 恢复预算]
    TASK --> REG[SkillRegistry<br/>技能注册 · 调用 · 忙时拒绝 · 取消]
    REG --> ATT[SkillAttempt<br/>前置检查 · 资源申请 · 请求与会话管理]
    ATT --> STRATEGY[调用感知 / 导航 / 操作策略]
    STRATEGY --> CHECK[结果评估<br/>执行反馈 + 真实观测 + 持物证据]
    CHECK --> ATT
    ATT -->|结果与任务事实| TASK
    TASK -->|有界重试 / 换站位 / 换策略| REG
    ATT -->|抓稳后移交| HOLD[执行层的持续持物会话]
```


**读图：** 首期可用显式任务状态机；SkillAttempt 管一轮请求与物理收敛。所有权详见架构第 3/6 节。

### 2. 观测与环境：提供同一时间语义下的可信输入

```mermaid
flowchart TB
    RAW[驱动输入<br/>雷达 · RGB-D · IMU · 关节 / 力 / 手部反馈] --> HISTORY[状态历史与时间管理<br/>时钟来源 · TF · 数据年龄]
    HISTORY --> OBS[ObservationAssembler<br/>按消费者要求对齐 · 缺失与有效性标记]
    OBS --> USE[导航 / 规划 / VLA / 技能验收]
    OBS --> DET[YOLO<br/>饮料身份与分割 mask]
    DET --> POSE[FoundationPose<br/>位姿估计 / 跟踪]
    OBS -->|同帧 RGB-D 与内参| POSE
    ASSET[对象模型 · 标定 · 静态站点] --> POSE
    ASSET --> SCENE[SceneStore<br/>对象位姿 · 持物事实 · 场景版本]
    POSE -->|带时间与坐标的结果| SCENE
    VERIFIED[技能验证的抓取 / 释放事件] --> SCENE
    SCENE --> VIEW[规划碰撞场景 / 导航运输约束 / 任务视图]
```


**读图：** 关节历史与图像采样时间关联；物体位姿及持物事实按来源维护。详见架构第 3/5 节。

### 3. 导航与操作策略：生成候选动作

```mermaid
flowchart TB
    GOAL[技能目标 + 观测 + 场景 / 本体配置] --> NAV[导航适配<br/>激光定位 · Nav2 · 站位 · 到站反馈]
    GOAL --> PLAN[传统操作策略<br/>MoveIt 路径 · 连续 IK / Servo<br/>掌心与手指抓取阶段协调]
    GOAL --> BUILD[VLA ObservationBuilder<br/>图像 / 状态 / 语言 · 模型预处理版本]
    BUILD --> CLIENT[异步推理客户端<br/>请求身份 · 超时 · 取消]
    CLIENT <--> SERVER[远端 VLA 模型服务]
    CLIENT --> DECODE[ActionDecoder<br/>动作维度 · 单位 · 绝对 / 增量 · 时间锚点]
    HUMAN[遥操作设备] --> TELE[遥操作适配<br/>掌心目标 · 手指重定向 / 手型映射]
    PLAN --> ACTION[操作动作候选 → 第 4 层]
    DECODE --> ACTION
    TELE --> ACTION
    NAV --> BASE[速度提议 → 第 4 层独立速度合同]
```


**读图：** 策略只生成候选；规划、VLA 与遥操作复用同一执行入口。候选组件和版本依据见建设计划。

### 4. 统一执行：决定谁能动、何时动、如何收敛

```mermaid
flowchart TB
    SOURCE[操作动作候选] --> ADAPT[ActionAdapter<br/>语义校验 · 按需运动学转换 · 命名维度]
    ADAPT --> PREP[TimelinePreparer<br/>时间对齐 · 过期裁剪 · 衔接 / 约束检查<br/>100 Hz 逻辑参考候选]
    PREP --> COMMIT[ExecutionSession<br/>资源租约 / epoch · 有效期 · revision 复核<br/>唯一权威时间轴提交]
    COMMIT -.->|边界状态与锁定前缀快照| PREP
    COMMIT --> BUFFER[已提交的不可变参考缓冲]
    BUFFER --> SAMPLE[ReferenceSampler<br/>按已提交曲线插值 · 各后端周期采样]
    SAMPLE --> JOINT[第 5 层臂手位置适配]
    VELOCITY[底盘速度提议] --> GATE[同一执行管理下的速度门控<br/>权限 · 来源时效 · 限制 · 超时]
    GATE --> BASE[第 5 层移动适配]
    COMMIT <--> HOLD[HeldObjectSession<br/>持续持物 · 保持资源 · 显式移交]
    FEEDBACK[真实反馈 / 故障 / 缓冲余量] --> WATCH[执行监测与停止管理<br/>取消 · 缺帧 · 超时 · 有界恢复]
    WATCH --> COMMIT
    WATCH --> GATE
    WATCH --> HOLD
    WATCH --> RESULT[分阶段执行反馈 → 技能与日志]
```


**读图：** 正常终点与断流分开，变速要更新权威时间轴；首期后端选择见架构 7.2，完整规则见执行契约。

### 5. 硬件适配：隔离不同机器人与设备协议

```mermaid
flowchart TB
    EXEC[执行层的获准目标] --> CONTRACT[RobotBackend 公共边界<br/>能力查询 · 命名映射 · 状态 / 故障 · 停止 / 保持]
    CONTRACT --> ARM[手臂 / 身体适配<br/>单位 · 顺序 · SDK / 传输协议]
    CONTRACT --> HAND[灵巧手适配<br/>独立驱动 · 机械耦合 · 模式 / 周期]
    CONTRACT --> BASE[轮式底盘适配<br/>速度 · odom · 停稳反馈]
    ARM --> LOW[已有底层位置控制器<br/>CSP 及实际总线周期]
    HAND --> HANDLOW[手部控制器<br/>实际协议与反馈待核实]
    BASE --> BASELOW[底盘控制器<br/>可用接口待核实]
    LOW --> STATE[带时间与来源的真实状态 → 观测 / 执行层]
    HANDLOW --> STATE
    BASELOW --> STATE
    CONTRACT -.-> BIP[后续双足后端<br/>行走 · 支撑 / 平衡 · 全身协调]
    SENSOR[传感器设备] --> DRIVER[传感器驱动 → 第 2 层原始数据]
```


**读图：** 同一资源选定一个实际后端；手臂/手周期分别声明。图中的抽象接口不等于强制新增进程。

### 6. 公共运行支撑：配置、模式、能力与生命周期

```mermaid
flowchart TB
    CONFIG[defaults / robot / site / task 配置] --> SNAP[ConfigStore<br/>校验 · 不可变快照 · generation / hash]
    PROFILE[RobotProfile / HandProfile<br/>模型 · 关节映射 · 限制 · 标定 · 能力] --> SNAP
    SNAP --> RUNTIME[RobotRuntime<br/>驱动 / 算法 / 通信对象装配]
    RUNTIME --> SUPER[RuntimeSupervisor<br/>模式 · 启停 · 依赖就绪 · 健康]
    HEALTH[状态新鲜度 · 心跳 · 资源 / 故障] --> SUPER
    SUPER --> READY[按模式与技能判定可用能力]
    READY --> TASK[任务 / 技能准入]
    READY --> EXEC[执行层许可与撤销]
    SUPER --> SWITCH[模式切换 / 关闭<br/>禁新输入 → 停止或移交 → 确认 → 重建 / 退出]
```


**读图：** 模式和能力按技能判定；控制周期不等待运行监督。配置与部署详见架构第 8 节。

### 7. 日志与数据：运行可诊断，示教可训练

```mermaid
flowchart TB
    EVENT[任务 / 技能 / 控制权事件] --> REC[异步 Recorder<br/>有界队列 · 时间与身份关联 · 丢失统计]
    SAMPLE[图像 / 状态 / 力 / 手部反馈] --> REC
    ACTION[遥操作原始输入 / 模型输出<br/>处理后参考 / 实际下发] --> REC
    VERSION[本体 / 模型 / 软件 / 配置版本] --> REC
    REC --> RUN[运行证据包<br/>manifest · events · probes · bags · summary]
    RUN --> DEBUG[诊断与回放<br/>轨迹对比 · 故障时间线 · 资源分析]
    RUN --> EP[Episode 管理<br/>任务 / 语言 / 阶段 / 接管 / 结果标注]
    EP --> PROCESS[离线处理<br/>时间对齐 · 质检 · 标签构造 · 独立采集单元切分]
    PROCESS --> DATASET[版本化数据集<br/>与训练端匹配的 LeRobot 格式]
    DATASET --> TRAIN[训练 / 离线评估 / 受控验证]
    TRAIN --> RELEASE[显式发布模型版本 → 推理服务]
```


**读图：** 记录输入、处理后参考、下发和实测的区别；采集 profile 与数据语义见架构第 9 节。

### 8. 与六层总图的映射

| 本文职责组 | 六层总图归属 |
|---|---|
| 1 任务与技能 | 应用交互层＋任务技能层 |
| 2 观测环境、3 导航操作策略 | 算法能力层；观测数据可直接到消费者 |
| 4 统一执行 | 控制执行层 |
| 5 硬件适配 | 驱动适配层及硬件边界 |
| 6 公共运行、7 日志数据 | 跨层公共平台 |

逻辑角色可以是类/库，不按图中数量创建节点或包。首期装配以[架构第 11 节](软件架构设计.md)为准。
