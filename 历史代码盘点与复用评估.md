# 历史代码盘点与复用评估

日期：2026-09-16。范围：`bindu/历史代码/`，已纳入后补的 `代码/` 目录（补充明细见第7节）。状态：完成目录/压缩包盘点、关键调用链审查和有限离线验证；**没有启动机器人、ROS 服务、远端推理或历史启动脚本，没有修改历史源码或压缩包。**

## 1. 结论

**已有一套覆盖控制、规划、任务、感知接入、遥操作和采集的历史工程基础，不需要把所有模块从零重写。它尚不是适配当前 Orin、满足新执行契约的可部署系统。**

最值得优先复用的是：

1. **独立 C++ 自适应插值器**：有点到点、30/100 Hz 流式输入、不规则输入及变频仿真，本次重新编译并通过 6 个既有场景。
2. **任务协议、注册分发、任务目录、调试工具**：已有清晰函数边界，4 个既有纯协议测试通过；可抽取逻辑，通信层按 ROS 2 契约适配。
3. **机器人模型与 OCS2/ros2_control 资产**：此前未展开，实际保存在内层 `ros2_ws.zip` 中；能减少重新建模和搭控制框架的工作。
4. **物体位姿到掌心/末端目标的转换、分段动作与灵巧手动作模板**：可用于首个固定物体、固定站位的抓取原型。
5. **采集同步、HDF5/JPEG 存储、VLA 状态/命令通信框架**：有实质实现，但需要修错、迁移 ROS 2 和重定义灵巧手动作语义。

后补目录进一步提供了 **ROS 1 导航实现、Pinocchio/CasADi 连续 IK 与 VR、YOLO 软管分割、FoundationStereo 深度估计、cuRobo 源码/模型和 ACT 训练评估代码**。此前“未找到导航实现/YOLO/通信依赖”的判断已部分被新证据替代。

尚不能记为具备：语音、FoundationPose 推理后端、饮料识别模型、完整多指示教数据管线、统一的高频执行/控制权/停止闭环，以及当前机器人上的端到端任务。ROS 1 导航和软管感知虽有实现，但尚未适配并验收当前 Orin/ROS 2 环境。

文中“快速复用”指在明确边界下抽取、适配的成本较低，**不等于复制后即可上真机**。没有据此给出未经验证的工期或成功率。

## 2. 实际有哪些资产

### 2.1 目录与版本

| 路径，均相对 `历史代码/` | 内容 | 建议用途 |
|---|---|---|
| `legs_necks_control/` | 独立 C++ 腰腿/颈部控制、Ti5 CAN 驱动、插值器、仿真、回放分析与历史 CSV | 插值算法优先抽取；控制器/驱动按硬件匹配复用 |
| `leg_control/legs_control_second/legs_necks_control/` | 上述工程另一份副本 | 去除隐藏目录、build*/log/缓存后的59个文件逐字节一致；只选一个维护入口，暂不删除 |
| `leg_control/legs_Control_first/` | EtherCAT 库、IPC/Qt 控制台、腰腿遥操作滤波、头部串口、底盘遥控、AgileX SDK/Tracer ROS 包 | 旧本体驱动/调试参考；不能整套当公共平台迁入 |
| `leg_control/taihu_control` | 无扩展名的 ZIP，232 个文件条目 | 归档，不是可执行控制程序 |
| `代码+视频/ros2_ws/new_version/` | cuRobo ROS 2 规划、轨迹执行、ZMQ 桥、目标序列、任务服务、RViz | 规划适配和上层任务的主要审查候选 |
| `代码+视频/ros2_ws/old_version/` | OCS2 上层控制、真机桥、较完整的多机器 API 服务与测试 | 保留作协议和行为对照；其中并不包含全部 OCS2 底层源码 |
| `代码+视频/ros2_ws/真机代码/niic/zmq_dexbot/` | ROS 1 状态采集、VLA 通信、导航/ACT/感知 API、进程编排 | 迁移通信、数据语义和设备映射，外部算法服务需另补 |
| `代码+视频/ros2_ws/真机代码/fun/` | ROS 1 双臂与手部发布、100 Hz 调度、限速限加速度、预设动作、`slrHardware/slrKernel` | 历史本体适配候选；公共算法与本体驱动分开 |
| `代码+视频/ros2_ws/ros2_ws.zip` | **未展开的旧完整工作空间**：23,337 个文件条目，其中 `src/` 下 4,572 个；另有 build/install/log | 模型、ROS 2 控制器、仿真和 LeRobot 资产的重要来源 |
| `代码/` | 后补7个 ZIP 与《开源软件清单.xlsx》：导航、VR/IK、软管感知/规划、ACT、真机旧快照、时间同步及测试 | 并非单纯重复包，详见第7节 |
| `代码+视频.zip` 及其他 ZIP、视频 | 多层归档、版本快照、演示视频 | 保留溯源；视频未观看，不据此认定功能已在当前机器人验证 |

首批统计口径：排除隐藏目录、`build*`、`install`、`log`、`__pycache__` 等，已展开源码共 **1,337 个文件，其中 Python 304 个**；包含第三方/厂商代码和重复副本，不代表自研规模。首批17个 ZIP，后补7个，当前已覆盖 **24 个 ZIP 格式归档**。后补包中另外检查873个源码成员，统计独立保留，不与展开文件混算。

机器可读索引：[展开源码及 SHA-256](过程记录/附件/2026-09-16-历史代码盘点/展开源码索引.csv)、[压缩包软件包索引](过程记录/附件/2026-09-16-历史代码盘点/压缩包软件包索引.csv)、[统计与范围](过程记录/附件/2026-09-16-历史代码盘点/盘点统计.json)。

### 2.2 内层 ZIP 中的重要内容

以下路径相对 `ros2_ws.zip!ros2_ws/src/`，目前仍在压缩包中：

| 资产 | 已看到的实现 | 边界 |
|---|---|---|
| `arms_ros2_control/controller/ocs2_arm_controller/` | OCS2 控制器、Hold/Home/MoveJ/OCS2 状态、MPC 线程、策略求值；`StateOCS2.cpp:144`、`CtrlComponent.cpp:303` | 连续控制有代码基础；未编译验证、未证明当前机械臂效果 |
| `arms_ros2_control/hardwares/topic_based_ros2_control/` | ROS 2 SystemInterface 的 read/write、JointState 状态/命令话题 | 是 ROS 2 话题适配层，不等于 EtherCAT/CSP 驱动或实时保证 |
| `arms_ros2_control/command/arms_target_manager/` | 末端目标、交互标记、VR 输入处理 | 可复用目标交互逻辑；不是完整多指遥操作系统 |
| `robot_descriptions/humanoid/Huawei/huawei_description/` | URDF、灵巧手模型、网格、OCS2/ros2_control 配置；`dexhand.yaml` 配置 100 Hz | 校验与当前机器的关节、尺寸、耦合、TCP、标定是否一致；不能用模型默认值替代实测 |
| `lerobot_ros2/` | Robot/Camera 插件、仿真采集/推理/训练入口、数据查看工具 | 源码引入的 `ros2_robot_interface` 和 LeRobot 子模块源码未随该 ZIP 提供；不是开箱可用训练环境 |
| `ocs2_ros2/`、其他 `robot_descriptions/` | 大量框架、机器人示例及第三方依赖 | 通用资产；不能把示例机器人数量算成平台已支持本体数量 |

归档 README 描述的基线包含 **Jazzy / Ubuntu 24.04 / Python 3.12**，LeRobot 安装说明还固定了 CUDA/PyTorch 版本。这些是历史说明，不能直接用于用户的 Ubuntu 22 / Orin 环境。未据此变更系统版本。

### 2.3 哪个版本应作为起点

- `new_version/service_runtime` 的 35 个 Python 文件中，31 个与旧服务目录相同，4 个有差异；不是两套完全独立系统。新版不含旧目录中的全部 NIIC/SAN/4090 服务，任务目录仍会调用这些外部服务。
- 展开版与 `new_version-8-30.zip` 的 77 个可比较 `.py/.sh/.yaml/.md` 文件中，68 相同、9 不同；与 `new_version-8-31.zip` 中的 77 个，65 相同、12 不同。两包都没有该范围内仅存于压缩包的文件。**不能只按日期把 8-31 版认定为稳定版。**
- 建议以展开代码进行修复与抽取，压缩包中的模型/控制器按需恢复到独立工作区；记录来源路径和哈希。原始目录、ZIP 和历史日志保留，不在本轮移动、合并或清理。

## 3. 功能与复用清单

便于定位，定义三个目录简称：

- **N**：`代码+视频/ros2_ws/new_version/`
- **R**：`代码+视频/ros2_ws/真机代码/`
- **Z**：`代码+视频/ros2_ws/ros2_ws.zip!ros2_ws/src/`

| 功能 | 代码证据及已实现内容 | 当前缺口 | 复用判断 |
|---|---|---|---|
| 在线插值/平滑 | `legs_necks_control/.../adaptive_interpolator.cpp`：位置及可选速度/加速度目标、目标年龄外推、速度/加速度/jerk 限制；独立标准 C++ | 多轴同步、有效期/断流、实际周期、输出导数一致性、轨迹偏差、缓存替换、跟踪反馈均需专项验收 | **最高优先，算法内核易抽取**；不能直接宣称整个执行层已完成 |
| 腰腿/颈驱动 | `DexbotController`：命令、周期线程、反馈、限位、状态；`Ti5Driver`：读位置/速度/电流、写命令 | 驱动库为 x86-64；SDK/总线行为和当前本体匹配未知；断开 SDK 不等于受控停止已证明 | 有条件复用到对应本体后端 |
| 双臂位置通路 | R 的 `fun/robot/1-huawei_pub_state.py`、`3-huawei_pub_cmd.py`、`slrHardware/slrKernel` | ROS 1、固定左右各7轴；后补 `v3.4.zip` 已提供 `zmq_tools` 源码和 `logging-mp` 依赖声明，安装/接口版本仍待验证 | 协议/映射参考；先复用当前已工作的底层 |
| 双臂限速滤波 | R 的 `fun/robot/joint_acc_limiter.py` | 固定 dt，输入首次直接初始化，未含 jerk/失效策略；不是完整反馈控制 | 可作对照基线，不默认叠加到新执行链 |
| cuRobo 规划 | N 的规划节点；后补软管包提供 cuRobo 源码、标准 `huawei_wheel_dual_arm.yml`、URDF/碰撞球 | 新版专用 `_fp.yml` 仍未找到；两版末端命名不同，不能直接改文件名替换；CUDA/Orin 未测 | **规划与模型可配套恢复**，统一执行逻辑另按契约适配 |
| OCS2 连续控制 | Z 的 OCS2 控制器、ROS 2 硬件接口及本体配置 | 归档依赖、构建版本、观测/命令语义与新契约需核对 | 作为连续目标方案的已有候选，不再假设需要从零实现 |
| 物体位姿接入 | N 的 `foundationpose_zmq_to_pose.py`：ZMQ 结果→PoseStamped、TF 转换及诊断 | 推理引擎/权重/物体资产不在包内；外参/时间戳需实测 | **接入桥可快速复用**；不算已具有完整 FoundationPose |
| 抓取目标与序列 | N 的 `object_pose_to_eef_target.py`、两类 `*_sequence_to_eef_targets.py`、抓取偏移 YAML：物体/末端相对变换、分段目标、容差、重试 | 固定目标资产/偏移，本体相关；缺少饮料实例身份、接触/持物确认和统一生命周期 | **固定饮料原型可优先改造** |
| 灵巧手 | R 的 `fun/robot/vr_hand.py`、`hand_open_close_controller.py`：手型、捏取、开闭、旋拧等位置模板和命令发布 | 未确认当前手型、关节/单位映射；模板不等于闭环抓取、力控或人体多指重定向 | **匹配同款手时快速复用模板/驱动边界** |
| 任务编排 | N 的 `service_runtime/common`、`main_client`：请求/回复 schema、API 注册、进程/日志、dry-run、逐步调试、VLM 流程分支；主目录 41 个 TaskDef | 固定桶/门/推车/管件任务；41 个定义不是41项验证完成技能。缺控制权、取消/幂等和持物结果合同 | **公共协议、任务步定义与调试工具优先抽取** |
| VLM 决策 | N 的 `server_4060/vlm_decision/`：图像请求及 next/retry/cancel 流程 | 默认启动含 `--vlm-test-mode`，真实模型路径另需配置 | 可作任务判断候选；VLM 判断不等于 VLA 动作策略 |
| VLA 客户端 | R 的 `niic/zmq_dexbot/zmq_niic*/`：相机/关节打包、ZMQ 发布/请求回复、接收动作、丢弃部分过期/积压消息、频率延迟统计 | ROS 1；模型端/权重不在此；尚无完整 action chunk 时间轴、接管/取消和高频执行合同 | 通信与观测封装可迁移；算法执行仍需补齐 |
| 示教采集 | R 的 `.../common/collect_data.py`：多相机及关节按时间同步、队列/丢帧诊断、JPEG/HDF5、任务文字 | 保存函数错误；手为每侧1维；标签来自下一帧状态，未记录完整原始命令/执行命令/实测；缺数据质检与目标训练格式验收 | **很有价值，但修复语义后再采新数据** |
| 导航/底盘 | 后补 `导航-源码.zip` 有 Livox/FAST-LIO、move_base/AMCL/TEB 配置、地图/航点、到点/相对移动/视觉误差微调服务；另有 Tracer/AgileX 驱动 | 主链 ROS 1；默认地图缺失、命名空间/重试/重复航点需处理；当前底盘及地图匹配未知 | **导航学生可从已有系统恢复与迁移起步**，无需重写到点业务逻辑 |
| 日志与诊断 | `common/process.py` 子进程日志；ZMQ 频率延迟；采集同步诊断；C++ 插值 CSV/分析；ROS1 电流记录脚本 | 未形成统一 task/attempt/session/command 关联、配置快照、故障包、回放与保留策略 | 工具可复用，公共日志系统还需整合 |
| 连续 IK / VR | 后补 `v3.4.zip`：Pinocchio FK、CasADi IK、上次解热启动/平滑目标、VR 相对位姿、模式切换及限速 | 默认本体碰撞对为空；多指 IK 类未见静态实例化调用；未测实时频率/当前设备 | **优先复用双臂示教和连续 IK 逻辑**，不能视为已通过碰撞和多指联调 |
| 软管分割 / 深度 / 抓取 | 后补软管包：YOLO/UNet、FoundationStereo、点云处理、目标生成、cuRobo | 是软管深度图分割；缺配置引用的 Stereo checkpoint；不是 FoundationPose 或饮料类别识别 | 复用感知框架/标定及通信，饮料模型仍需独立建设 |
| ACT | 后补 `act.zip`：训练、评估、action chunk 和 temporal aggregation | 未随附任务权重和训练数据；不等于语言条件 VLA 或原远端 ACT 服务已复原 | 学习策略基线，可保留训练/评估代码 |
| 语音、FoundationPose 推理、旧 MoveIt 工程 | 全部已提供目录中仍未找到完整对应实现；有 FP 接入及 `moveit_interface.srv` | FoundationStereo 不等于 FoundationPose；service 定义不等于 MoveIt 工程 | 继续按计划建设/补齐 |

## 4. 已确认的阻塞与易误判点

### 4.1 插值已有，但调度与失效管理未完成

- C++ 仿真使用 2 ms 步长，本次 6 个场景通过。证明这组离线算法场景可运行，**不证明 Orin 上 500 Hz 实际周期、整机轨迹安全或多轴误差**。其中多步点到点场景允许约 0.044 rad 的最终误差仍通过，旧测试标准不能直接作为抓取精度标准。
- `DexbotController::controlLoop()` 在每次 read/write 后再 `wait_for(control_period)`（`dexbot_controller.cpp:345`），实际周期包含 IO/计算耗时；配置 2 ms 不能直接视为固定 500 Hz。
- 插值器使用目标年龄进行外推，但接口中没有目标 TTL、资源代次或统一取消/停止语义；非零导数目标断流后的处理需执行层规定。
- README 仍写“还没有固定周期循环”，而源码已启动 `control_thread_`；应以源码和验证结果为准，不能照抄 README 状态。

### 4.2 cuRobo 执行器不能直接承担新统一执行层

证据：N 的 `robot_bridge/trajectory_executor.py`。

- 默认 50 Hz（第 239 行附近）；`_tick()` 第 194 行起按 `while` 补发所有已到期点，**没有按实际时刻连续采样/插值**。本次隔离验证复现一次延迟回调连续发出 3 个点。
- 第 198–200 行在末点发布后立刻报告 `completed`，不是依据真实终点误差确认完成。上层部分序列还有容差判断，但不能补成执行器通用的真实到达保证。
- `validate_trajectory()` 接受相等的 `time_from_start`；只检查通用位置范围及相邻步长，没有本体逐关节速度/加速度/jerk 验收。
- 通过 `header.frame_id` 传 `segment_id`，将坐标系字段用于任务身份；新协议应显式区分。
- `zmq_real_robot_command_bridge.py:249` 还可追加 EMA，后端另有滤波。多处滤波会改变轨迹及结束时刻，不能默认全部启用。
- 规划器初始化的环境仅有占位障碍（`curobo_ros2_motiongen.py:279`）；本次未看到实时场景障碍更新闭环，不能据“用了 cuRobo”就认定已能避开桌面/环境障碍。

### 4.3 数据采集先修两类问题

- **明确代码错误**：`zmq_niic_new/common/collect_data.py:254` 在 `for cam_name in camera_names` 中使用未定义的 `camera_name`。AST 核对该函数及模块均没有这个绑定；进入该非空相机保存分支会发生 NameError。语法解析通过不能发现此错误。
- **动作语义不满足当前目标**：`HAND_DOF = 1`（第 44 行附近），每侧手取一个值；`process():741` 用下一帧实测 `qpos` 作为上一帧 observation 的 action；`save_data():272` 又将当前 `qpos` 写入名为 `joint_action` 的数据集。下一状态标签可作为明确约定的数据方案，但不能被误称为原始指令或完整多指动作。要同时保留 operator intent、接受后的执行目标和真实状态，并固定训练转换规则。

### 4.4 环境与反馈完整性

- `zmq_niic_new` 的入口仍 `from bridge import ...` / `from state_producer import ...`，这些文件已移到 `common/`，随带入口脚本没有补全该路径；独立复制此目录仍需修复导入/打包。
- `legs_necks_control/usrlib/` 的4个 `.so` 均为 **Linux x86-64**；不能直接用于 Orin ARM64。旧 EtherCAT 库有 aarch64 版本，但也不能仅凭架构一致认定 ABI/内核/设备匹配。
- 后补软管包已提供 cuRobo 源码、标准模型配置和 URDF，`v3.4` 补出真机中转源码。新版专用 `_fp.yml`、LeRobot 接口子模块及部分远端脚本仍未找到；`logging-mp` 只有依赖声明，不能宣称部署已补齐。
- ROS 2 状态桥对到达的字典做局部更新，再发布完整缓存（N 的 `zmq_real_robot_state_bridge.py:79`）；部分关节持续缺失仍可能被完整消息掩盖。要按关节记录有效性和源时间，不能只检查“最近收过一帧”。
- 部分历史 API 存在固定远端地址、绝对路径和明文凭据。抽取时改为部署配置并剥离凭据；本报告和索引不复制其值。
- 历史 CSV/图片和 README 的真机声明只能作为线索。本轮没有核实对应机器人、固件、日期、参数和采集过程，不升级为当前本体验证结果。

## 5. 建议的复用顺序

### 第一批：进入离线最小闭环

1. **冻结来源**：保留原目录；以本报告索引、哈希记录候选文件。只恢复需要的模型/控制器源码，避开历史 build/install 缓存。
2. **先提取插值内核与任务协议**：保留既有测试，再接[执行契约](动作执行契约.md)的有限轨迹和一种在线模式；补齐有效期、停止、真实到达、重定时和具名关节限制。插值内核不绑定 Ti5/ROS/ZMQ。
3. **单臂单手模拟回放**：核对 Huawei 模型与当前机器人；先加载具名命令/反馈录制片段，复现接近、手型、保持、取消和断流。固定手型模板可先复用，禁止用默认开手代替通用停止。
4. **接当前已工作的机器人接口**：使用实测确认的后端；不为了迁入历史驱动重新接管已工作的关节控制。

### 第二批：规划与早期数据并行恢复

- **规划/IK**：cuRobo 源码/标准配置、OCS2、Pinocchio/CasADi 连续 IK 均有基础；MoveIt 仍是候选。局部连续 IK 与全局路径规划分开评估，先用同一模型/目标/执行合同测试构建、成功率、跳变、碰撞、时间和 Orin 资源，不为每种算法新建一套执行系统。
- **采集/VLA**：迁移 ROS 2、修复保存路径、明确实际手部自由度，建立小样本数据往返读取与动作回放；再接模型服务和 chunk 时间轴。Z 中 LeRobot 代码可参考，但缺失依赖先补齐。
- **感知**：复用 FP 结果接入、抓取偏移和新增 YOLO/点云框架；重新准备饮料识别、FP 推理及物体资产。现有软管模型不直接当作饮料模型，第一阶段仍可用已知位姿验证操作。
- **导航学生**：先恢复后补导航工程的包依赖与启动关系，离线核对航点、地图、TF 和 service，再迁移到 ROS 2 并交付到点/取消/恢复；无需先承担操作或VLA，也不应直接复制旧参数上机。

由此调整的是“从哪些资产开始实现”，并不改变 P0–P6 验收标准，也不把桶/门/管件等历史任务加入首期饮料范围。

## 6. 本轮验证与覆盖边界

| 检查 | 结果 | 不能证明什么 |
|---|---|---|
| 304 个已展开 Python 的 AST 解析 | 无语法错误 | 不能证明导入、名称绑定、第三方依赖或实际运行正确 |
| 新服务公共协议，复用旧目录4个纯测试 | 4/4 通过 | 未测试 ZMQ网络、外部 handler、取消/控制权和真机 |
| C++ 插值源码＋原有仿真主程序独立编译 | 编译成功，6/6 原有场景通过 | 未链接 Ti5/yaml-cpp/ROS；未构建完整控制工程；未覆盖物理控制误差 |
| 提取原执行器校验/调度函数，普通数据对象离线运行 | 复现同时间戳被接受、单回调3点补发、末点发送即完成 | 这是函数级复现，不是 ROS 集成测试 |
| 保存函数 AST 名称检查、二进制 `file` 检查 | 定位保存错误及 x86-64 库 | 未实测 HDF5 采集和 ARM64 SDK |
| ZIP 中央目录＋关键源文件选择性读取 | 找到遗漏模型与框架、核对部分缺失依赖 | 没有遍历审查全部第三方源码、全部嵌套压缩内容或观看视频 |

复现入口（仓库根目录）：

```sh
python3 bindu/过程记录/附件/2026-09-16-历史代码盘点/inspect_assets.py
python3 bindu/过程记录/附件/2026-09-16-历史代码盘点/verify_offline.py
```

脚本只读历史源码；输出到附件目录，C++ 编译及仿真在独立临时目录运行。验证环境：本机 macOS ARM64、Python 3.9.6、Apple clang 21；结果及源码指纹见[离线验证结果](过程记录/附件/2026-09-16-历史代码盘点/离线验证结果.json)。这次结论是资产与复用评估，不是完整软件安全审计或整机验收。

## 7. 后补 `代码/`：新增内容、重复版本与剩余缺口

本节记录第二批输入；上文功能结论已同步修正。首批附件及当时的过程记录保留为历史快照。原 ZIP 和 XLSX 均只读，未解压进部署目录、加载权重或运行系统安装脚本。

### 7.1 内容重复比对

按源码扩展名筛选，排除隐藏、构建、日志、缓存与符号链接；单成员不超过4 MB。与首批展开源码和首批归档内源码做 SHA-256 对比，不按同名判断重复。

| 新包 | 检查源码成员 | 首批存在相同内容 | 首批未见相同内容 | 应如何处理 |
|---|---:|---:|---:|---|
| `act.zip` | 20 | 0 | 20 | 新增 ACT 基线，独立保存 |
| `chrony.zip` | 1 | 1 | 0 | 已有时间同步脚本的重复副本 |
| `robot.zip` | 49 | 45 | 4 | 多数重复，但有行为差异；不可整包覆盖 |
| `v3.4.zip` | 46 | 2 | 44 | 新增 VR、连续 IK、通信实现，优先保留 |
| `双臂测试.zip` | 1 | 0 | 1 | 简短测试变体，不算新的控制能力 |
| `导航-源码.zip` | 334 | 61 | 273 | 大量新增导航业务/定位代码；部分驱动与旧包重复 |
| `软管识别操作-源码.zip` | 422 | 15 | 407 | 大量新增感知/cuRobo及第三方源码，独立保存 |
| **合计** | **873** | **124** | **749** | 包含第三方及样板代码，不能将比例当功能复用率 |

“首批未见相同内容”既可能是新模块，也可能是旧文件修改版，不表示749个新功能。新包相互之间的重复不包含在此列口径。

`robot.zip` 与首批 `真机代码/fun/robot/` **同路径**比较：41个相同、8个不同，没有仅存于新包的源码路径。和上表45个内容相同不矛盾：部分文件在首批其他副本中有相同内容。8个差异涉及手型动作、手部开闭时序、关节速度、启动/记录脚本、API工具和进程日志。例如新包手臂速度常量为0.25 rad/s，原展开版本为0.40 rad/s；这属于行为差异，不能因为文件名相同删除其中一版，也不意味着较大值更正确。

证据：[逐文件内容指纹与重复来源](过程记录/附件/2026-09-16-历史代码补充盘点/补充源码与重复索引.csv)、[统计/差异路径/静态检查](过程记录/附件/2026-09-16-历史代码补充盘点/补充盘点统计.json)。本轮不选定未经验证的“最终稳定版本”。

### 7.2 导航：已有真实实现，优先交给导航学生梳理

包内前缀 `导航-源码.zip!nav_src/src/`：

- `NEXTE_Sentry_Nav/sentry_nav/launch/all_in_one.launch` 组合雷达、LIO相关启动、move_base、到点和底盘微调服务。
- `sentry_nav/src/nav_server.py`、`TaskTransfer.py` 提供航点管理和 move_base action 调用；`self_frame_mover_server.py` 提供相对平移/旋转；`camera_object_error_service/.../object_error_server.py` 提供视觉目标误差与底盘微调。这些补上首批只有 API 调用而无实现的缺口。
- 有 Livox ROS Driver 2、FAST-LIO/定位代码、Point-LIO、点云转图、速度平滑、地图/航点和 Tracer SDK；主导航工程仍是 ROS 1，雷达驱动同时含 ROS 1/2 启动文件不意味着整套已经 ROS 2 化。

恢复前必须处理的具体问题：

1. `sentry_localize.launch` 默认引用 `FAST_LIO/PCD/scans.pcd`，该文件不在包中；有其他点云地图，但不能不核对坐标就替换。该启动文件还注释了 global_localization/transform_fusion 节点，需明确实际采用的定位/TF链。
2. `all_in_one.launch` 把节点命名为 `nav_server`，而服务使用 `~go_to_order`。经此 launch 启动时，应核对 `/nav_server/go_to_order` 与旧 API 默认 `/auto_2d_nav_task/go_to_order` 的差异。
3. `TaskTransfer.py` 对未成功的 action 在循环中每次最多等待300秒后重发，无整体重试上限；新技能接口需要有界重试、取消、失败原因。
4. 航点加载遍历全部 JSON；order 1、2、4、5、6 各存在两份文件。必须先消除歧义并记录当前地图的唯一航点集。

以上是源码/启动配置发现，未运行导航。历史雷达、底盘、外参与地图不能自动升级为当前机器人事实。

### 7.3 连续 IK 与遥操作：补齐了重要开发起点

**工程定位：双臂 VR 遥操作。** 包内前缀 `v3.4.zip!v3.4/`。精选包已收录62个源码/配置/说明/模型文本路径；因按功能分别归入03遥操作、02 IK、09通信、10模型和08数据目录，统一阅读入口见[精选包说明](代码参考包/README.md)。

主链按源码可追到：VR 输入 → 左右臂模式与相对目标 → 连续 IK/关节限速 → ZMQ 中转 → 机器人客户端及可选 ROS 桥接。以下是模块检查结果，尚非当前机器联调结论：

- `ik_solver/pinocchio_utils.py:661` 定义 FK/IK 工具：末端位置/姿态目标、关节限位、与上次关节解的平滑代价；`solve_ik():727` 使用上次解作为初值。这是已有连续目标求解方案，不必等待旧 MoveIt 才能研究连续 IK。
- `robots/huawei_robot_server_new.py` 有 VR 相对目标、模式切换、空闲保持、关节限速和 CSV；`pyzmq_base/src/zmq_tools/` 补出首批缺少的通信源码。`logging-mp` 出现在 requirements/安装说明中，仍需验证安装源与版本，未把依赖声明当作已安装。
- 有手部模型、25个手部点的数据通道及 `HandControllerProcess` 多指 IK 类；但对本包 Python AST 检查未见该类的直接构造调用，默认双臂服务也未接入完整手指命令闭环。结论是**多指实现片段存在，完整多指遥操作/采集仍未证实**。
- `NewHuaweiArmFixHand` 的 `collision_pairs = []`（第1050行）。上层传入 `check_collision=True` 不能弥补空碰撞对配置，需补模型碰撞配置与实际测试；平滑代价也不能证明100 Hz求解能力。

优先提取：具名关节模型、纯 FK/IK、VR 目标映射和通信编码。旧程序中的键盘管理、进程清理、日志和硬件写入分离后再接统一执行入口。

### 7.4 软管工程：补齐 YOLO 和 cuRobo，仍有部署缺口

包内前缀 `软管识别操作-源码.zip!pipe_ros_ws/`：

- `src/perception_pkg/scripts/detection_module.py:94` 将**深度图归一化后转成三通道图像**送入 YOLO，默认单类/单实例；包内有 `yolo26n-seg.pt` 和 UNet 权重文件。此处只确认文件与调用代码存在，没有加载权重或验证训练类别/准确率，不应当作现成饮料检测器。
- `stereo_module.py:68` 调 FoundationStereo 生成深度；配置的 `fs_model/big_model/model_best_bp2.pth` **未随包提供**，目录仅见 `cfg.yaml`。因此整条默认感知链还不能直接运行。FoundationStereo 与 FoundationPose 是不同任务，FP 位姿估计后端仍缺。
- `pipe_client.py:102` 从重建软管点云中选点、施加偏移和预设姿态后发布目标。这是软管场景的抓取启发式，不是通用物体6D位姿估计或饮料抓取算法。
- `src/plan_pkg/curobo/` 中有 cuRobo 源码、`huawei_wheel_dual_arm.yml`、URDF和碰撞球，补上此前“cuRobo仅有外部依赖”的部分缺口；专用 `_fp.yml` 仍未找到。标准版末端为 `left_hand_base_link/right_hand_base_link`，新版控制节点期望 `l_link_ee/r_link_ee`，须明确映射及TCP差异后适配。
- `perception_pkg/CMakeLists.txt` 仅安装 `pipe_server.py`，该脚本导入的同目录检测/深度等模块和资产未完整安装；随带 `setup.py` 也未提供完整打包。历史 build/install 目录或开发路径可能掩盖这个问题，应在干净环境检查安装产物。

**建议复用方向**：先恢复模型和规划离线样例，再提取检测/点云/服务框架。饮料识别另建任务模型；已有 RGB-D 原生深度可作为独立验证输入，但不能据此宣称默认 Stereo 链已经修复。

### 7.5 ACT、清单与验证边界

- `act.zip` 包含 `policy.py`、`imitate_episodes.py`、DETR模型、训练/评估与时间聚合逻辑。没有随附任务 checkpoint；也没有补出首批外部 `ACT_open_door/act_async_infer.sh` 的完整部署链。ACT 可作为学习策略基线，不能自动视为语言条件 VLA。
- `chrony.zip` 是相同时间同步脚本；本轮没有执行其 install、systemctl 或调时操作。时钟同步能力仍需现场测量。
- 只读核对[开源软件清单](历史代码/代码/开源软件清单.xlsx)的 `开源软件清单!A3:F15`。它是历史声明，不是锁定环境或许可证审计证据：B10 写作“Jazzy/Numble”；E12 声称 FoundationStereo 权重内置，但配置所需权重未见；B14/E14 的版本/文件名声明不等于当前已安装版本或模型验证。未修改工作簿、未判断其“已履行”法律结论。
- 本轮873个源码成员的654个 Python 文件 AST 检查全部通过；未导入 ROS/CUDA/Pinocchio、未编译导航、未训练/推理、未测 Orin，不复跑未变化的首批插值测试。复现命令：

```sh
python3 bindu/过程记录/附件/2026-09-16-历史代码补充盘点/inspect_supplement.py
```

下一步建议仍是单臂单手执行主线，同时导航学生从已有导航工程恢复与ROS 2迁移起步。新增资产减少实现量，不降低执行、日志、数据和真机验收要求。

## 八、分类去重后的源码精选交付

按用户要求，已将有参考价值的部分整理为独立[ZIP 与使用说明](代码参考包/README.md)。10类覆盖控制插值、规划 IK、遥操作灵巧手、任务技能、导航底盘、感知几何、学习策略 VLA、数据日志、通信同步、模型配置。

精选1,961个路径，按完整字节内容精确去重为1,509份独立内容，移除452份重复；不同参数和行为版本保留。统计范围包括源码/配置/模型文本/许可，不等于自研规模或全历史目录重复率。已过滤缓存、运行日志、权重、视频、二进制、私钥和完整 googletest 副本；原始历史资产保留。

包内来源索引记录原始路径、归档成员、原始与整理后指纹、规范存放路径和脱敏次数；恢复脚本可补回重复文件的所选路径，不自动安装依赖或补齐未收录资产。它是迁移参考包，不是即开即用的机器人工作区。验证命令、结果见[整理附件](过程记录/附件/2026-09-16-代码参考包/整理验证结果.json)。

## 九、GUI 设计与交互参考

GUI 也是有价值的复用资产，详见[GUI 参考导读](代码参考包/GUI参考导读.md)。本地 `legs_control_second` 的插值设计文档包含点对点 GUI 输入约定；当前找到的完整 Qt 界面实现位于 `legs_Control_first/examples/qt-exipc`，已收录精选包。布局、反馈面板、手动目标与模式切换值得复用，ROS 1/IPC/串口耦合及请求/实际反馈混用需要改造。本次未运行 GUI，不把源码审查等同界面或真机验收。
