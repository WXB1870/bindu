# 项目上下文

更新：2026-09-16。本文只维护用户确认的需求、现有条件和未知输入。建议与选型见[架构](软件架构设计.md)/[计划](一期建设计划与开源选型.md)，实施状态见[当前进度](当前进度.md)。

## 已确认目标与范围

- 建立基于 ROS 2、硬件无关且可复用的长程移动操作系统；首期轮式人形完成简单移动抓取，后续支持双足。
- 首个任务：语音指定饮料 → 导航 → 感知 → 灵巧手抓取 → 持物返回；末端均使用灵巧手。
- 需要建设语音、激光雷达导航、YOLO / FoundationPose 感知、运动规划/FK/IK 等基础技能；同一技能允许传统与 VLA 等策略。
- 规划或 VLA 提供/转换为 100 Hz 关节目标，下层动态自适应插值和平滑后对接已有位置控制。实际硬件周期不等于已确定的上层频率。
- VLA 在机器人本地只运行客户端，调用远端模型服务，不部署本地 VLA 推理；优先复用历史客户端，包含动作接续/平滑接入与异常日志。本批优先接入 Pi，ACT 暂不做；用户确认先兼容历史“发布观测”和“模型端拉取观测”两种 ZMQ 模式，实际服务地址尚未确定。已有遥操作设备，示教数据用于 VLA，包含采集、处理与质检管线。
- 全链路日志需可关联、可回放；恢复包括重试/重观察、调整站位/更换策略。
- 软件架构先行，以 `bindu/` 为主工作区，长期维护计划、上下文与过程记录；早期 Galbot Markdown 单独放在 `参考资料/Galbot/`。
- 骨架必须支持后续能力扩展；Ubuntu 首版应保留任务、观测、策略、执行、本体与数据边界，不能局限为一条关节执行演示。具体实现与验收仍为架构/计划中的建议。
- 总工程名为 `bindu`；用户要求按可替换模块组织核心代码，遵循清晰的文件夹层级。用户授权本地实现后复制到指定开发机验证。
- 用户进一步要求拆分过宽的机器人后端职责，并初始化 Git 仓库；设备驱动与命令路由/反馈汇总分别维护；进一步要求评估 core 边界，优先按已有模块进行文件夹命名与分类。
- 面向实验室算法替换，IK、VLA、视觉感知、导航、规划、语音交互与遥操作按能力各自独立目录；实现状态与目录是否存在分开说明。
- 用户要求控制文件生成频率，优先更新现有文档，减少零散 Markdown；执行规则统一维护在根目录 `AGENTS.md`。

## 已有条件与尚未核实部分

| 项目 | 已确认 | 未知 / 限制 |
|---|---|---|
| 开发机 | 用户指定 `wxb@192.168.1.116`；本轮实测 x86_64、Ubuntu 24.04.4、ROS 2 Jazzy、Python 3.12.3；工程为 `/home/wxb/bindu` | 与机器人 Orin 的系统不同，不能据此确认 ARM64 / Humble / JetPack 兼容性；不记录登录凭据 |
| 计算平台 | Jetson Orin，Ubuntu 22，ROS 2 | Orin 型号/内存、系统小版本、JetPack/CUDA/TensorRT、ROS 发行版 |
| 场地/传感器 | 平地室内、已有地图、3D 雷达、RGB-D、力传感器；IMU 仅来自雷达/相机 | 地图格式、设备型号/安装、时间同步、力传感器位置与语义 |
| 关节控制 | 底层已做好；上层关节位置接口、此前说明 CSP；部分情况可读力矩 | SDK 模式、内部队列/插值、真实位置/故障反馈、停止/保持、总线周期 |
| 移动控制 | 此前提及底盘速度，后又说明“只有关节级位置接口” | 底盘速度接口当前是否开放、odom、运动学，不能当作已接通 |
| 灵巧手 | 所有末端采用灵巧手 | 型号、独立驱动/耦合、命令/反馈/周期、触觉能力 |
| 遥操作 | 已有设备，服务于 VLA 示教 | 是否支持手指、原始数据/动作接口 |
| 软件与人员 | 用户提供 `历史代码/`；一名学生学习导航；此前提及另有旧 MoveIt | 本次找到控制/规划/任务/采集源码及归档模型，详见下文；仍未找到完整旧 MoveIt 工程，无当前导航验收证据 |

可读力矩不等于可下发力矩；设备存在不代表反馈接口已可用。`参考资料/Galbot/` 的资料不是当前机器的实现证据。

### 本地源码事实（2026-09-16 补充）

用户提供的[历史代码](历史代码盘点与复用评估.md)包含：C++ 插值/腰腿颈控制、cuRobo ROS 2 适配、任务服务、ROS 1 真机/灵巧手/VLA/采集，以及归档 OCS2、ros2_control、Huawei 模型和 LeRobot 插件。补充的 `历史代码/代码/` 还提供 Pinocchio/CasADi 连续 IK、VR/zmq_tools、ROS 1 导航、YOLO/UNet 软管感知、FoundationStereo 和 ACT。FoundationStereo 不等于 FoundationPose；完整语音、FoundationPose 后端及旧 MoveIt 工程仍未找到。源码存在不等于当前 Orin/机器已部署或验收。

用户要求将有价值代码单独分类并去重，已生成[源码精选包](代码参考包/README.md)，保留来源、版本差异及复用边界，原始历史资产不改。用户补充强调腰腿工程的 GUI 设计也可参考；已核实第二版的输入约定和第一版 Qt 实现，详见[GUI 导读](代码参考包/GUI参考导读.md)。具体功能和验证证据在盘点报告维护。

## 还需补齐的输入

1. 表中计算平台与 SDK/反馈/停止能力；归档 URDF 与当前本体的对应关系、SRDF、关节限制、掌心/指尖及标定。
2. 地图、底盘、传感器/时间同步条件；力反馈的实际可用性。
3. 饮料目录与物体资产，取物点、返回点、最终交付方式。
4. 遥操作手指能力、目标 VLA/数据 schema、远端模型服务的可用地址、协议、版本与观测/动作样例。
5. 允许延迟/路径偏差、跟踪误差、保持/停止和抓取验收条件；旧 MoveIt 版本/API 与历史结果。

固定取物区、返回起点、返回后继续持物及 Humble 基线均是建议，未获用户确认。首期任务不自动包含递交、人定位、开门、手内翻转或双足平衡开发。
