# 当前进度

更新：2026-09-17。当前完成 P0 的 Ubuntu 模拟骨架首版及替换/故障实验，准备接入真实能力；P1–P6 未验收。目标机测试进程均已关闭，无后台部署任务。

## 已完成

- **Bindu 模拟工程**：按已有职责分为 `shared/`、`tasks/`、`control/`、`hardware/`、`data/`、`capabilities/`、`integration/`；移除职责过宽的 `bindu_core`，共11个可构建 ROS 2 包。公共契约与实现分离，执行通过 `ExecutionIO` 接入设备；驱动与命令路由/反馈汇总在硬件包内独立子目录。IK/语音交互/遥操作仍仅预留，详见[实现边界](软件架构设计.md#854-当前代码组织与替换边界)。
- **Pi 客户端首版**：`bindu_vla/pi/` 已接入历史 ZMQ 发布观测/拉取观测两种模式，ROS `PiSession` 通过公共控制权和在线目标连接模拟执行器；配置、映射、过期/重放拒绝、取消和诊断已实现。当前每个会话控制一个关节组；ACT 暂不做，本地不运行 VLA 推理。边界与入口见[架构8.5.4](软件架构设计.md#854-当前代码组织与替换边界)及[运行命令](README.md#pi-客户端运行入口)。
- **最新模拟验证（9月17日）**：Ubuntu 24.04.4 / ROS 2 Jazzy / x86_64，11包构建、28项模块测试、14个 Pi ZMQ/ROS 场景通过，110个工程文件两机指纹一致。首次13场景中2次会话启动被拒绝；原测试仅等待自身发出观测，未确认客户端实际就绪，且旧版无具体拒绝原因。已增加就绪服务/拒绝诊断，测试等待实际就绪，并以延迟观测场景验证“先拒绝、就绪后接纳”。未放宽新鲜度约束。[构建](artifacts/build-pi-2026-09-17.log) · [模块测试](artifacts/pi-simulation-2026-09-17-final/unit.log) · [最终结果](artifacts/pi-simulation-2026-09-17-final/results.json) · [源码指纹](artifacts/pi-simulation-2026-09-17-final/environment.json) · [当日过程](过程记录/2026-09-17.md)。
- **既有骨架回归**：9月16日原有15个 ROS 场景通过，见[结果](artifacts/pi-regression/results.json)；本次修改限于 Pi 就绪/诊断与测试，本轮未重跑这15项，不作为新版本的全量回归结论。
- **验证范围**：两种模拟策略 × 两套具名关节配置、任务取消、感知迟到/过期、后端拒绝、反馈丢失、记录器/感知进程退出、100 Hz 在线目标与旧控制权拒绝、标准 launch 完整流程；新增只让灵巧手拒绝/丢失反馈，验证其他设备的新反馈不能掩盖失联。模拟结果与 episode 明确标识；反馈丢失返回“停止未确认”。不等于真实抓取或实时性能验收。

- **设计与图示**：架构 v0.3、执行契约 v0.2、P0–P6 计划及科研 PNG 已形成；完整设计未全部验收。图示统一在[系统架构图册](系统架构图.md)，原独立审查报告已并入[当日审查详情](过程记录/2026-09-16.md#审查详情)。
- **历史资产复用准备**：源码盘点、分类去重精选包和 GUI 来源复核已完成；模块清单、统计、来源与离线验证集中见[盘点报告](历史代码盘点与复用评估.md)，使用入口见[代码参考包](代码参考包/README.md)。原始资产保持不变，历史实现尚未完成当前本体适配。
- **文档整理**：两组同职责文档已归并，独有图示和审查证据保留；需求、架构、契约、计划、盘点及决策继续独立维护。协作规则见[AGENTS.md](AGENTS.md)，历史过程见[当日记录](过程记录/2026-09-16.md)。

历史代码接入位置与语音入口见[架构8.5.5–8.5.6](软件架构设计.md#855-设备接入拆分与历史代码落点)。完整语音、实际感知模型、真实 Pi 服务联调/相机及本体映射适配、训练数据管线仍未完成。Pi 网络客户端已接入，测试使用模拟远端和模拟设备，不能等同真实 Pi 抓取验收。

## 未完成与待验证

- 历史代码尚未适配当前 ROS 2 契约；模拟后端已有，真实本体适配、C++ 自适应插值、完整手部保持/并发资源管理仍未完成。P1–P6 未验收。
- 真实硬件反馈、停止、队列/插值、底盘/odom 和灵巧手能力未核实。
- 候选软件在目标 Orin 上的兼容性、并发资源与效果未验证。
- 历史资产已分类提取，工程化适配与缺陷修复尚未完成；完整旧 MoveIt 工程未找到。ROS 1/ROS 2、x86-64/ARM64、缺失模型配置和依赖必须分开处理。

## 下一步：Ubuntu 可扩展骨架 → 替换验证 → 实际能力接入

近期批次见[计划4.1.1](一期建设计划与开源选型.md#411-当前实现批次以-v34-遥操作验证首条执行链)。骨架首轮模拟验证已完成，以下为下一批：

1. 以当前骨架为基线，把 v3.4 连续 IK/遥操作封装为首个真实输入适配模块，先向模拟后端输出具名关节目标；模型、依赖与输入样例核对先行，不直接启动历史控制脚本。
2. 核对当前本体接口、模型与环境；恢复 v3.4 必要模块，将 VR/连续 IK 先接模拟后端，再替换为当前机器人适配器，先单臂后灵巧手。
3. 复用最小 GUI 调试交互，完成遥操作抓取、保持/放下，以及可重新加载的图像/臂手 episode。
4. 导航学生独立推进到点/取消/停稳；Pi 的两种 ZMQ 模式已通过模拟服务验证，下一步取得真实端点/协议样例、接相机和关节映射后联调；ACT 暂缓。规划、感知、移动任务和真实模型策略按 P3–P6 汇合。

下一步依赖输入集中在[上下文](项目上下文.md)。第一个软件里程碑已完成首轮模拟实验，不能称长期稳定或全架构验收；第一个真机里程碑仍为遥操作抓取及可用数据。当前记录仅覆盖收到的事件、位姿和关节消息，尚无相机同步、完整 VLA 数据转换或 Orin 实测。

本地 Git 仓库已在 `bindu/` 建立；历史记录中的“尚无仓库”描述保留当时状态。源码与验证指纹见上述证据入口。
